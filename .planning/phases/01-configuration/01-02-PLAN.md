---
phase: 01-configuration
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/lightrag_langchain/config.py
  - tests/test_config.py
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04, CONF-05]

must_haves:
  truths:
    - "settings.pg.host returns the value of PG_HOST from .env as a str"
    - "settings.llm.model returns the value of LLM_MODEL from .env as a str"
    - "settings.embedding.dim returns 1024 by default when EMBEDDING_DIM is not set in .env"
    - "settings.reranker.binding returns empty string by default (rerank disabled by default)"
    - "settings.query_params.top_k returns 40 by default (matches upstream LightRAG)"
    - "Missing a required field (e.g., PG_HOST) during Settings() instantiation raises SettingsError with a message naming PostgreSQL and the missing field"
    - "Multiple validation failures across different config groups are reported together in a single error message grouped by [PostgreSQL], [LLM], etc."
    - "Setting settings.pg.host = 'new' after instantiation raises an error (frozen=True per D-12)"
    - "Printing or repr of settings.llm.binding_api_key shows '**********' not the raw key value"
    - "Each of the 5 sub-models can be instantiated independently via constructor arguments without needing the full Settings class"
    - "from lightrag_langchain.config import settings succeeds when .env is valid (module importable per SC #1)"
    - "QueryParamsConfig raises ValidationError when max_entity_tokens + max_relation_tokens >= max_total_tokens (cross-field invariant per D-08)"
  artifacts:
    - path: "src/lightrag_langchain/config.py"
      provides: "Typed configuration API: 5 sub-models + Settings singleton + categorized error formatting"
      exports: ["settings", "Settings", "PgConfig", "LlmConfig", "EmbeddingConfig", "RerankerConfig", "QueryParamsConfig", "SettingsError"]
      contains: "class Settings(BaseSettings)"
    - path: "tests/test_config.py"
      provides: "Comprehensive test coverage for all CONF-01 through CONF-05 and all 4 success criteria"
      contains: "def test_"
      min_lines: 200
  key_links:
    - from: "src/lightrag_langchain/config.py"
      to: "pydantic_settings.BaseSettings"
      via: "import and subclass"
      pattern: "from pydantic_settings import BaseSettings"
    - from: "src/lightrag_langchain/config.py"
      to: "pydantic.SecretStr"
      via: "field type annotation"
      pattern: "SecretStr"
    - from: "tests/test_config.py"
      to: "src/lightrag_langchain/config.py"
      via: "import settings and sub-models"
      pattern: "from lightrag_langchain.config import"
    - from: "tests/test_config.py"
      to: "tests/conftest.py"
      via: "pytest fixture"
      pattern: "temp_env_file"

threat_model:
  trust_boundaries:
    - boundary: ".env file -> pydantic-settings BaseSettings parser"
      description: "Raw secret values parsed from .env must be protected from exposure in logs, errors, and repr/str output"
    - boundary: "config.py module import -> Settings singleton"
      description: "Fail-fast validation at import time must not leak secret values in error messages"
    - boundary: "Runtime config access -> downstream phases"
      description: "frozen=True must prevent runtime mutation of config values after validation"
  stride_register:
    - threat_id: "T-01-03"
      category: "Information Disclosure"
      component: "src/lightrag_langchain/config.py — SettingsError._format_validation_error()"
      disposition: "mitigate"
      mitigation_plan: "Error formatter groups errors by config group name only; error messages reference field names (e.g., 'pg.host') but NEVER include raw field values or connection strings. Pydantic's built-in ValidationError.errors() msg field does not include input values for SecretStr fields."
    - threat_id: "T-01-04"
      category: "Information Disclosure"
      component: "src/lightrag_langchain/config.py — SecretStr fields"
      disposition: "mitigate"
      mitigation_plan: "All password/API key fields use SecretStr type. Pydantic's default __repr__ and __str__ for SecretStr display '**********'. get_secret_value() is the only programmatic access path — never called in error formatting or logging code."
    - threat_id: "T-01-05"
      category: "Tampering"
      component: "src/lightrag_langchain/config.py — frozen=True models"
      disposition: "mitigate"
      mitigation_plan: "All 5 sub-models and top-level Settings use model_config=ConfigDict(frozen=True) per D-12. Any attempt to set a field after instantiation raises pydantic.ValidationError, preventing runtime config tampering."
    - threat_id: "T-01-06"
      category: "Spoofing"
      component: "src/lightrag_langchain/config.py — extra='forbid' on Settings"
      disposition: "mitigate"
      mitigation_plan: "Settings model_config sets extra='forbid' which rejects unknown .env variables. This prevents typo'd or malicious env vars from being silently consumed. Legitimate extra vars cause a clear SettingsError."
    - threat_id: "T-01-SC"
      category: "Tampering"
      component: "pip install pydantic-settings, pydantic, python-dotenv"
      disposition: "mitigate"
      mitigation_plan: "All packages pass slopcheck [OK] per RESEARCH.md Package Legitimacy Audit; no [SUS] or [SLOP] packages"
---

<objective>
Implement the typed configuration API using pydantic-settings: 5 nested sub-models, Settings singleton, fail-fast validation with categorized error summary, SecretStr for sensitive fields, and frozen immutability. Write comprehensive tests covering all CONF requirements, success criteria, and user decisions.

Purpose: This is the foundation every downstream phase depends on. All Phase 2-6 code accesses configuration through `from lightrag_langchain.config import settings`.
Output: src/lightrag_langchain/config.py (implementation) and tests/test_config.py (test suite)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-configuration/01-CONTEXT.md
@.planning/phases/01-configuration/01-RESEARCH.md

# RESEARCH.md provides the exact implementation patterns:
# - Lines 196-285: Pattern 1 — Nested sub-model with independent env_prefix and full field definitions
# - Lines 287-337: Pattern 2 — Categorized error summary (_format_validation_error)
# - Lines 339-358: Pattern 3 — SecretStr safe logging
# - Lines 360-364: Anti-patterns to avoid (BaseSettings for sub-models, missing nested_model_default_partial_update, model_post_init on frozen models)
# - Lines 385-421: Common pitfalls (nested_model_default_partial_update omission, frozen+after-validator, extra="forbid")
# - Lines 519-532: Open Questions (reranker binding="" = disabled, embedding dim=1024 per D-06)

# Plan 01 created tests/conftest.py with temp_env_file fixture and tests/__init__.py. Read these to understand available test infrastructure.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1: Write test_config.py — comprehensive test suite (RED phase)</name>
  <files>tests/test_config.py</files>
  <read_first>
    - tests/conftest.py (created in Plan 01 — understand the temp_env_file fixture signature and return type)
    - .planning/phases/01-configuration/01-RESEARCH.md (lines 566-583 for the complete test-to-requirement map listing all test function names and their coverage; lines 425-439 for independent sub-model instantiation test pattern; lines 441-455 for ValidationError.errors() structure)
    - .planning/phases/01-configuration/01-CONTEXT.md (D-05 through D-12 for behaviors that tests MUST verify; Claude's Discretion for SecretStr masking behavior)
    - .planning/phases/01-configuration/01-VALIDATION.md (lines 39-53 for the per-task verification map with exact test function names and automated commands)
    - .planning/REQUIREMENTS.md (CONF-01 through CONF-05 for exact required field lists and default values)
  </read_first>
  <behavior>
    - Test 1 (test_pg_config_instantiation): PgConfig(host="localhost", port=5432, user="u", password=SecretStr("p"), database="db") → host=="localhost", port==5432, user=="u", database=="db", isinstance(password, SecretStr). Covers CONF-01.
    - Test 2 (test_pg_config_default_port): PgConfig(host="h", user="u", password=SecretStr("p"), database="db") → port==5432 (default). Covers CONF-01.
    - Test 3 (test_llm_config_instantiation): LlmConfig(binding="openai", binding_host="https://api.openai.com/v1", binding_api_key=SecretStr("sk-test"), model="gpt-4o-mini") → binding=="openai", model=="gpt-4o-mini", temperature==0.0 (default), max_tokens==9000 (default). Covers CONF-02.
    - Test 4 (test_embedding_config_default_dim): EmbeddingConfig(binding="openai", binding_host="https://api.openai.com/v1", binding_api_key=SecretStr("sk-test"), model="text-embedding-3-small") → dim==1024 (default per D-06). Covers CONF-03.
    - Test 5 (test_reranker_config_all_defaults): RerankerConfig() → binding=="", binding_host=="", binding_api_key is SecretStr, model=="", min_rerank_score==0.0. Covers CONF-04.
    - Test 6 (test_reranker_config_with_values): RerankerConfig(binding="cohere", binding_host="https://api.cohere.com", binding_api_key=SecretStr("k"), model="rerank-v3", min_rerank_score=0.5) → all fields match input. Covers CONF-04.
    - Test 7 (test_query_params_defaults_match_lightrag): QueryParamsConfig() → top_k==40, chunk_top_k==20, max_entity_tokens==6000, max_relation_tokens==8000, max_total_tokens==30000, cosine_threshold==0.2, kg_chunk_pick_method=="VECTOR". Covers CONF-05.
    - Test 8 (test_token_budget_invariant_passes): QueryParamsConfig(max_entity_tokens=6000, max_relation_tokens=8000, max_total_tokens=30000) → does NOT raise (6000+8000=14000 < 30000). Covers D-08.
    - Test 9 (test_token_budget_invariant_fails): QueryParamsConfig(max_entity_tokens=20000, max_relation_tokens=15000, max_total_tokens=30000) → raises ValidationError (35000 >= 30000). Covers D-08.
    - Test 10 (test_token_budget_invariant_edge_equal): QueryParamsConfig(max_entity_tokens=15000, max_relation_tokens=15000, max_total_tokens=30000) → raises ValidationError (30000 >= 30000). Covers D-08.
    - Test 11 (test_independent_submodel_instantiation): Each of the 5 sub-models instantiated independently via constructor args, field values verified. No Settings class involved. Covers SC #4.
    - Test 12 (test_frozen_prevents_mutation): PgConfig(...) then pg.host = "new" → raises ValidationError. Repeat for LlmConfig, EmbeddingConfig, QueryParamsConfig. Covers D-12.
    - Test 13 (test_secret_str_masking_in_repr): str(PgConfig(host="h", user="u", password=SecretStr("secret"), database="db")) does NOT contain "secret". Covers Claude's Discretion.
    - Test 14 (test_secret_str_masking_in_str): repr(PgConfig(...)) does NOT contain the raw password. Covers Claude's Discretion.
    - Test 15 (test_settings_from_valid_env): Use temp_env_file fixture to create .env with all 12 required fields (PG_HOST/PORT/USER/PASSWORD/DATABASE + LLM_BINDING/HOST/API_KEY/MODEL + EMBEDDING_BINDING/HOST/API_KEY/MODEL). Instantiate Settings(_env_file=env_path). Verify settings.pg.host, settings.llm.model, settings.embedding.dim==1024, settings.reranker.binding=="", settings.query_params.top_k==40. Covers SC #2, CONF-01 through CONF-05.
    - Test 16 (test_missing_required_field_raises_settings_error): Create .env with PG_HOST and PG_USER but missing PG_PASSWORD and PG_DATABASE. Settings(_env_file=env_path) → raises SettingsError. Error is NOT raw pydantic ValidationError — it is SettingsError per D-07. Covers SC #3.
    - Test 17 (test_error_message_grouped_by_config_group): Settings(_env_file=missing_pg_env_path) → SettingsError message contains "[PostgreSQL]" group header. Covers D-07.
    - Test 18 (test_error_message_lists_multiple_missing_groups): Create .env missing both PG_DATABASE and LLM_MODEL. Settings(_env_file=env_path) → SettingsError message contains both "[PostgreSQL]" and "[LLM]" group headers. Covers D-07.
    - Test 19 (test_settings_extra_forbid): Create .env with valid config plus UNKNOWN_KEY=value. Settings(_env_file=env_path) → raises SettingsError about extra input. Covers extra="forbid".
    - Test 20 (test_import_module_succeeds): With a valid .env created via temp_env_file (all 12 required fields), `python3 -c "from lightrag_langchain.config import settings"` exits 0. Test uses subprocess.run or creates env, sets _env_file, and imports. Covers SC #1.
    - Test 21 (test_settings_frozen): After Settings(_env_file=valid_env), settings.pg.host = "new" → raises ValidationError. Covers D-12 on Settings level.
    - Test 22 (test_query_params_env_override): Create .env with TOP_K=60. Settings(_env_file=env_path) → settings.query_params.top_k==60 (overrides default 40). Covers CONF-05.
  </behavior>
  <action>
    Write tests/test_config.py with all 22 test functions listed in the behavior block. Structure:

    - Import pytest, SecretStr from pydantic, Path from pathlib.
    - Import sub-models and Settings from lightrag_langchain.config (these will NOT exist yet — tests are written first in TDD RED phase).
    - Import SettingsError from lightrag_langchain.config.
    - Import ValidationError from pydantic for catching expected validation failures.
    - Each test function is independent and uses either direct constructor args (for sub-model tests) or the temp_env_file fixture (for Settings integration tests).

    For sub-model tests (Tests 1-7, 11-14): Instantiate the sub-model directly with constructor arguments. Use SecretStr("value") for password/API key fields. Assert field values match input.

    For Settings integration tests (Tests 15-22): Use the temp_env_file fixture from conftest.py. Call temp_env_file(PG_HOST="localhost", PG_PORT="5432", ...) with all required fields, then Settings(_env_file=returned_path). For error tests, omit specific required fields.

    For module import test (Test 20): Use monkeypatch.setenv to set all required env vars, or create a helper that wraps the import in a subprocess to avoid state leakage between tests.

    Important: Tests must be written to FAIL before config.py is implemented. Do NOT create a placeholder config.py — let the tests fail on import with ModuleNotFoundError. The TDD GREEN phase (Task 2.2) implements config.py to make these tests pass.

    Use monkeypatch fixture from pytest directly in test functions that need env var manipulation (no need to define it in conftest.py — pytest provides it globally).
  </action>
  <acceptance_criteria>
    - tests/test_config.py contains 22 test functions with names matching the behavior block (test_pg_config_instantiation, test_llm_config_instantiation, etc.)
    - At least 2 tests for each CONF-01 through CONF-05 requirement
    - At least 1 test for each of SC #1, SC #2, SC #3, SC #4
    - At least 1 test for D-07 (categorized errors)
    - At least 2 tests for D-08 (token budget: pass and fail cases)
    - At least 3 tests for D-12 (frozen on sub-model, frozen on Settings, mutation raises)
    - At least 2 tests for SecretStr masking (str() and repr() do not expose raw value)
    - Running tests now fails with ModuleNotFoundError (config.py does not exist yet — this is correct TDD RED state)
    - Test file does NOT import from lightrag_langchain.config at module level if that would break collection — use function-level imports inside test bodies to allow test collection even when config.py is missing
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_config.py --collect-only 2>&amp;1 | grep -c 'test_' | xargs -I{} test {} -ge 20</automated>
  </verify>
  <done>22 test functions defined; test collection succeeds (imports are deferred); tests currently FAIL because config.py does not exist (RED phase complete)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2: Implement config.py — typed configuration API (GREEN phase)</name>
  <files>src/lightrag_langchain/config.py</files>
  <read_first>
    - .planning/phases/01-configuration/01-RESEARCH.md (lines 196-285 for Pattern 1 — complete sub-model definitions with field names, types, defaults, and env_prefix; lines 287-337 for Pattern 2 — _format_validation_error implementation; lines 339-358 for Pattern 3 — SecretStr safe logging rules; lines 360-364 for anti-patterns: BaseSettings for sub-models, missing nested_model_default_partial_update, model_post_init on frozen)
    - .planning/phases/01-configuration/01-CONTEXT.md (D-05 through D-12 for all locked decisions that must be reflected in the implementation)
    - tests/test_config.py (written in Task 2.1 — read to understand exact test expectations and import paths)
    - .planning/phases/01-configuration/01-RESEARCH.md (lines 385-421 for Common Pitfalls: Pitfall 1 nested_model_default_partial_update omission, Pitfall 2 frozen+after-validator, Pitfall 3 extra="forbid", Pitfall 4 Python version)
  </read_first>
  <behavior>
    - PgConfig (frozen BaseModel): 5 fields — host: str (required), port: int=5432, user: str (required), password: SecretStr (required), database: str (required). Model-level env_prefix NOT needed on BaseModel sub-models (parent Settings handles env var routing via field name matching).
    - LlmConfig (frozen BaseModel): 6 fields — binding: str (required), binding_host: str (required), binding_api_key: SecretStr (required), model: str (required), temperature: float=0.0, max_tokens: int=9000.
    - EmbeddingConfig (frozen BaseModel): 5 fields — binding: str (required), binding_host: str (required), binding_api_key: SecretStr (required), model: str (required), dim: int=1024 (per D-06).
    - RerankerConfig (frozen BaseModel): 5 fields — binding: str="" (empty=disabled), binding_host: str="", binding_api_key: SecretStr=SecretStr(""), model: str="", min_rerank_score: float=0.0. All defaulted so reranker is optional.
    - QueryParamsConfig (frozen BaseModel): 7 fields with LightRAG-matching defaults — top_k: int=40, chunk_top_k: int=20, max_entity_tokens: int=6000, max_relation_tokens: int=8000, max_total_tokens: int=30000, cosine_threshold: float=0.2, kg_chunk_pick_method: str="VECTOR". Has @model_validator(mode='after') check_token_budget that raises ValueError when max_entity_tokens + max_relation_tokens >= max_total_tokens.
    - Settings (frozen BaseSettings): 5 nested fields — pg: PgConfig, llm: LlmConfig, embedding: EmbeddingConfig, reranker: RerankerConfig=RerankerConfig(), query_params: QueryParamsConfig=QueryParamsConfig(). model_config=SettingsConfigDict(env_file=".env", env_nested_delimiter="__", nested_model_default_partial_update=True, extra="forbid", case_sensitive=False).
    - SettingsError(Exception): Custom exception class.
    - _format_validation_error(exc: ValidationError) -> str: Groups errors by config group using err["loc"][0] path element. Maps group keys to display names: pg→PostgreSQL, llm→LLM, embedding→Embedding, reranker→Reranker, query_params→QueryParams. Output format: "Configuration validation failed:\n\n[GroupName]\n  - field: message\n  - field2: message2\n". Error messages reference field names only, never raw values.
    - Module-level singleton: try Settings() except ValidationError as e: raise SettingsError(_format_validation_error(e)) from e. Assign result to module-level `settings` variable.
    - SecretStr for all password/API key fields — pydantic handles auto-masking in __repr__/__str__.
    - Anti-patterns avoided: sub-models are BaseModel (NOT BaseSettings), nested_model_default_partial_update=True is set, @model_validator(mode='after') only reads fields (does not mutate — avoids frozen conflict).
  </behavior>
  <action>
    Implement src/lightrag_langchain/config.py per D-01 through D-12. Single file ~100-150 lines per D-10.

    Imports: ConfigDict from pydantic, BaseSettings/SettingsConfigDict from pydantic_settings, SecretStr/BaseModel/ValidationError/model_validator from pydantic, defaultdict from collections.

    Field naming convention: snake_case Python fields map to UPPER_CASE env vars. pydantic-settings handles case-insensitive matching automatically via case_sensitive=False in SettingsConfigDict. For example, env var PG_HOST maps to Settings.pg.host because the Settings field is named `pg` and pydantic-settings strips the matching prefix. This works because pydantic-settings 2.x routes env vars to nested models by matching the field name as a prefix — PG_HOST → pg field → PgConfig.host.

    Define the 5 sub-models as frozen BaseModel classes with ConfigDict(frozen=True). Each sub-model is a standalone class that can be instantiated directly with constructor args for unit testing (SC #4).

    Define the SettingsError exception class (inherits Exception, no custom behavior needed — just a marker type).

    Define _format_validation_error(exc: ValidationError) -> str that processes exc.errors() list, groups by err["loc"][0], and formats categorized output. The GROUP_MAP dict maps "pg"→"PostgreSQL", "llm"→"LLM", "embedding"→"Embedding", "reranker"→"Reranker", "query_params"→"QueryParams". Error lines use the pattern "  - field_name: error_message".

    Define Settings(BaseSettings) with all 5 nested fields and the model_config described in the behavior block.

    At module bottom: wrap Settings() in try/except ValidationError to produce the settings singleton.

    After writing config.py, run the test suite from Task 2.1. Iterate until all 22 tests pass. The key pitfalls to watch for (per RESEARCH.md):
    - If sub-model defaults disappear when only partial env vars are set: verify nested_model_default_partial_update=True is present in SettingsConfigDict.
    - If @model_validator(mode='after') raises frozen error: ensure the validator only reads fields (raise ValueError, never try to mutate self.field = new_value).
    - If extra="forbid" rejects legitimate vars: ensure .env only contains known keys; .env.example defines the canonical set.
  </action>
  <acceptance_criteria>
    - src/lightrag_langchain/config.py exists and imports cleanly (python3 -c "import ast; ast.parse(open('src/lightrag_langchain/config.py').read())" succeeds)
    - config.py defines class PgConfig with fields host, port, user, password, database (CONF-01)
    - config.py defines class LlmConfig with fields binding, binding_host, binding_api_key, model, temperature, max_tokens (CONF-02)
    - config.py defines class EmbeddingConfig with field dim defaulting to 1024 (CONF-03, D-06)
    - config.py defines class RerankerConfig with all-defaulted fields (CONF-04)
    - config.py defines class QueryParamsConfig with 7 LightRAG-matching defaults and @model_validator for token budget (CONF-05, D-08)
    - config.py defines class Settings(BaseSettings) composing all 5 sub-models with env_file=".env" and nested_model_default_partial_update=True
    - config.py defines class SettingsError(Exception)
    - config.py defines function _format_validation_error(ValidationError) -> str with categorized group output (D-07)
    - config.py has module-level `settings = Settings()` wrapped in try/except (D-11, D-05)
    - All sub-models and Settings use ConfigDict(frozen=True) (D-12)
    - Password/API key fields use SecretStr type
    - All 22 tests from test_config.py pass (GREEN phase)
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/test_config.py -v</automated>
  </verify>
  <done>All 22 tests pass; config.py provides typed configuration API with fail-fast validation, categorized errors, SecretStr masking, frozen immutability, and module-level singleton</done>
</task>

<task type="auto">
  <name>Task 2.3: Verify — full test suite, import, and lint</name>
  <files>(no new files — verification only)</files>
  <read_first>
    - pyproject.toml (created in Plan 01 — verify ruff and pytest config sections exist)
    - .planning/phases/01-configuration/01-VALIDATION.md (lines 29-33 for sampling rate and phase gate commands)
  </read_first>
  <action>
    Run the full verification sequence:

    1. Install dependencies: pip install -e ".[dev]" (installs lightrag-langchain in editable mode plus pytest and ruff dev deps declared in pyproject.toml).

    2. Run full test suite: python3 -m pytest tests/ -v. All 22 tests must pass.

    3. Import check: python3 -c "from lightrag_langchain.config import settings, PgConfig, LlmConfig, EmbeddingConfig, RerankerConfig, QueryParamsConfig, Settings, SettingsError; print('All imports OK')". Must print "All imports OK".

    4. Lint check: python3 -m ruff check src/ tests/. Must exit 0 with no errors.

    5. Format check: python3 -m ruff format --check src/ tests/. Must exit 0.

    6. Verify edge cases manually:
       - Create a minimal valid .env with all 12 required fields, verify `python3 -c "from lightrag_langchain.config import settings; print(settings.pg.host)"` works.
       - Create an invalid .env missing PG_HOST, verify `python3 -c "from lightrag_langchain.config import settings"` raises SettingsError with categorized message.
       - Verify settings.pg.host = "new" raises error (frozen).

    If any check fails, fix the issue in config.py or test_config.py and re-run.
  </action>
  <acceptance_criteria>
    - pip install -e ".[dev]" succeeds
    - python3 -m pytest tests/ -v exits 0 with all 22 tests passing
    - python3 -c "from lightrag_langchain.config import settings, PgConfig, LlmConfig, EmbeddingConfig, RerankerConfig, QueryParamsConfig, Settings, SettingsError" exits 0
    - python3 -m ruff check src/ tests/ exits 0
    - python3 -m ruff format --check src/ tests/ exits 0
    - Invalid .env (missing required field) raises SettingsError with categorized error message
    - Frozen config prevents mutation at runtime
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/ -v &amp;&amp; python3 -m ruff check src/ tests/ &amp;&amp; python3 -m ruff format --check src/ tests/</automated>
  </verify>
  <done>Full test suite green (22/22); imports work; ruff check and format clean; frozen and error formatting verified manually</done>
</task>

</tasks>

<verification>
Phase gate — run before marking complete:
1. `python3 -m pytest tests/ -v` — all 22 tests pass
2. `python3 -m ruff check src/ tests/` — zero lint errors
3. `python3 -m ruff format --check src/ tests/` — zero format issues
4. `python3 -c "from lightrag_langchain.config import settings; print(type(settings.pg.host))"` — prints <class 'str'>
5. Verify categorized error by running: `PG_HOST='' python3 -c "from lightrag_langchain.config import settings" 2>&1 | grep -q '\[PostgreSQL\]'`
</verification>

<success_criteria>
- All 5 config groups (CONF-01 through CONF-05) are implemented as typed sub-models
- All 4 ROADMAP success criteria verified: module importable (SC #1), .env parsed with typed access (SC #2), missing config raises clear error (SC #3), independent sub-model testability (SC #4)
- All 12 locked decisions (D-01 through D-12) are reflected in the implementation
- Claude's Discretion items (SecretStr, safe logging, independent env_prefix) are implemented
- 22 tests cover all requirements, success criteria, and edge cases
- Config is frozen (D-12), fail-fast (D-05), with categorized errors (D-07) and cross-field validation (D-08)
</success_criteria>

<output>
Create .planning/phases/01-configuration/01-02-SUMMARY.md when done
</output>
