---
phase: 03-llm-integration
plan: 03
type: execute
wave: 1
files_modified:
  - src/lightrag_langchain/reranker.py
  - tests/test_reranker.py
autonomous: true
requirements:
  - LLM-03

must_haves:
  truths:
    - "create_reranker() dispatches to aliyun/cohere/jina adapter based on RERANK_BINDING config value"
    - "All three reranker backends return standardized [{index, relevance_score}] format"
    - "HTTP calls retry on 5xx/transport errors with exponential backoff (3 retries, 1s->2s->4s) and fail fast on 4xx"
    - "LightRAGReranker(BaseDocumentCompressor) wraps the Protocol reranker for LangChain ContextualCompressionRetriever compatibility"
  artifacts:
    - path: "src/lightrag_langchain/reranker.py"
      provides: "Reranker Protocol, ali_rerank/cohere_rerank/jina_rerank adapters, create_reranker factory, LightRAGReranker compressor"
      exports: ["Reranker", "create_reranker", "LightRAGReranker"]
      min_lines: 150
    - path: "tests/test_reranker.py"
      provides: "Unit tests for factory dispatch, response normalization, retry behavior, compressor integration"
      min_lines: 80
  key_links:
    - from: "src/lightrag_langchain/reranker.py create_reranker()"
      to: "src/lightrag_langchain/config.py RerankerConfig"
      via: "factory parameter type annotation"
      pattern: "config: RerankerConfig"
    - from: "src/lightrag_langchain/reranker.py LightRAGReranker.compress_documents"
      to: "langchain_core.documents.compressor.BaseDocumentCompressor"
      via: "class inheritance"
      pattern: "class LightRAGReranker(BaseDocumentCompressor)"
    - from: "src/lightrag_langchain/reranker.py _post_rerank"
      to: "tenacity.retry"
      via: "@retry decorator"
      pattern: "from tenacity import retry"
---

<objective>
Implement a multi-backend reranker integration with a unified typing.Protocol interface, three thin HTTP adapter functions (ali_rerank, cohere_rerank, jina_rerank), a factory dispatcher (create_reranker), and a LangChain BaseDocumentCompressor wrapper (LightRAGReranker). Write comprehensive unit tests covering factory dispatch, response format normalization, tenacity retry behavior, and Document compressor integration.

Purpose: Per D-05 through D-09, the reranker must support aliyun/cohere/jina backends interchangeably via RERANK_BINDING config, use httpx for HTTP calls, implement tenacity retry with exponential backoff consistent with Phase 2 pool.py, provide a dual-layer interface (raw Protocol for provider-agnostic calls + BaseDocumentCompressor for LangChain pipeline integration), and normalize all provider-specific response formats to a standard [{index, score}] format.

Output: `src/lightrag_langchain/reranker.py` with Reranker Protocol, 3 thin adapter functions, create_reranker() factory, and LightRAGReranker(BaseDocumentCompressor) class. `tests/test_reranker.py` with 8+ test cases covering LLM-03 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-llm-integration/03-RESEARCH.md
@.planning/phases/03-llm-integration/03-CONTEXT.md

# Upstream LightRAG reference — adapter patterns to replicate
@/Users/lizhouyang/llm/graphrag/LightRAG/lightrag/rerank.py    (ali_rerank L:475-512, cohere_rerank L:368-432, jina_rerank L:435-472, generic_rerank_api L:182-365)

# Existing codebase patterns
@src/lightrag_langchain/data/pool.py                           (tenacity retry pattern L:133-173)
@src/lightrag_langchain/config.py                              (RerankerConfig model L:109-121)
@tests/conftest.py                                             (mock_reranker_config, mock_httpx_client fixtures)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit tests for reranker factory, adapters, retry, and compressor</name>
  <files>tests/test_reranker.py</files>
  <read_first>src/lightrag_langchain/config.py, tests/conftest.py, /Users/lizhouyang/llm/graphrag/LightRAG/lightrag/rerank.py</read_first>
  <behavior>
    - Test 1 (test_create_reranker_dispatches_cohere): Given mock_reranker_config with binding="cohere", binding_host="https://api.cohere.com/v2/rerank" — when create_reranker(config) is called — then the returned object's internal adapter is the cohere adapter (verify by checking the adapter's default base_url matches cohere's endpoint).
    - Test 2 (test_create_reranker_dispatches_jina): Given mock_reranker_config with binding="jina" — then the returned reranker uses jina adapter.
    - Test 3 (test_create_reranker_dispatches_aliyun): Given mock_reranker_config with binding="aliyun" — then the returned reranker uses aliyun adapter.
    - Test 4 (test_create_reranker_unknown_binding): Given mock_reranker_config with binding="unknown_provider" — then create_reranker raises ValueError with message containing "unknown_provider".
    - Test 5 (test_ali_rerank_response_normalization): Given mock_httpx_client that returns aliyun-format response `{"output": {"results": [{"index": 0, "relevance_score": 0.95}]}}` — when ali_rerank() is called with documents=["doc1"] — then returns `[{"index": 0, "relevance_score": 0.95}]`.
    - Test 6 (test_cohere_rerank_response_normalization): Given mock_httpx_client that returns standard-format response `{"results": [{"index": 1, "relevance_score": 0.87}]}` — when cohere_rerank() is called — then returns `[{"index": 1, "relevance_score": 0.87}]`.
    - Test 7 (test_reranker_retry_on_5xx): Given mock_httpx_client where post() raises httpx.HTTPStatusError with status=503 on first call, succeeds on second — when a reranker adapter is called — then the call succeeds after retry. Verify the HTTP post was called at least twice.
    - Test 8 (test_reranker_no_retry_on_4xx): Given mock_httpx_client where post() raises httpx.HTTPStatusError with status=400 — when a reranker adapter is called — then the error propagates immediately without retry (post called exactly once).
    - Test 9 (test_lightrag_reranker_compressor): Given a mock Reranker (Protocol) that returns `[{"index": 0, "relevance_score": 0.9}, {"index": 1, "relevance_score": 0.5}]` — when LightRAGReranker.compress_documents is called with [Document("doc_A"), Document("doc_B")] and query="test" — then returns Documents sorted by relevance_score descending, each with `metadata["relevance_score"]` set.
    - Test 10 (test_reranker_async_signature): Given a mock Reranker — verify the rerank method signature is `async rerank(query: str, documents: list[str], top_n: int | None = None) -> list[dict[str, Any]]`.
  </behavior>
  <action>
    Create tests/test_reranker.py. Tests use `unittest.mock` (patch, AsyncMock, MagicMock) to mock httpx.AsyncClient and avoid real network calls.

    **For Tests 1-4:** Import create_reranker from the not-yet-existing module, use mock_reranker_config from conftest.py. Test dispatch by checking internal state (e.g., `reranker._base_url` matches the expected endpoint for that binding).

    **For Tests 5-6:** Mock httpx.AsyncClient.post to return specific response JSON. Call the adapter function directly (ali_rerank, cohere_rerank) with the mock injected or patched. Verify the returned list[dict] matches expected normalized format.

    **For Tests 7-8:** Use tenacity's retry behavior. Patch httpx.AsyncClient.post to raise HTTPStatusError on first call, succeed on second. Wrap the adapter call and verify it eventually succeeds (Test 7) or fails immediately (Test 8). For Test 8 with 4xx, verify the call count is 1 (no retry).

    **For Test 9:** Create a simple mock reranker class that implements the Protocol (returns hardcoded scores). Construct LightRAGReranker with it, call compress_documents, verify:
    - Returned documents are sorted by relevance_score descending
    - metadata["relevance_score"] is set on each document
    - Original page_content is preserved

    **For Test 10:** Use `typing.get_type_hints()` or `inspect.signature()` to verify the async method signature.

    After writing all 10 tests, run: `python -m pytest tests/test_reranker.py -x -q` — ALL tests MUST fail (ImportError for lightrag_langchain.reranker).
  </action>
  <acceptance_criteria>
    - CLI assertion: `python -m pytest tests/test_reranker.py -x -q 2>&1 | tail -5` shows all tests failing with ImportError
    - Source assertion: `grep -c "def test_" tests/test_reranker.py` >= 10
    - Source assertion: `grep -c "create_reranker" tests/test_reranker.py` >= 3
    - Source assertion: `grep -c "LightRAGReranker\|compress_documents" tests/test_reranker.py` >= 2
    - Source assertion: `grep -c "HTTPStatusError\|TransportError" tests/test_reranker.py` >= 2
    - Source assertion: `grep -c "relevance_score" tests/test_reranker.py` >= 4
    - Source assertion: `grep -c "Document" tests/test_reranker.py` >= 2
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_reranker.py -x -q 2>&1; echo "Exit: $?"; test $? -ne 0</automated>
  </verify>
  <done>tests/test_reranker.py exists with 10+ failing tests. All fail because reranker.py does not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Reranker Protocol, adapters, factory, and BaseDocumentCompressor</name>
  <files>src/lightrag_langchain/reranker.py</files>
  <read_first>tests/test_reranker.py, src/lightrag_langchain/config.py, src/lightrag_langchain/data/pool.py, /Users/lizhouyang/llm/graphrag/LightRAG/lightrag/rerank.py</read_first>
  <behavior>
    After implementation:
    - Tests 1-3 pass: factory dispatches to correct adapter by binding string
    - Test 4 passes: unknown binding raises ValueError
    - Tests 5-6 pass: aliyun response (output.results[...]) and standard response (results[...]) both normalize to [{index, score}]
    - Test 7 passes: 503 retries and succeeds
    - Test 8 passes: 400 propagates immediately, no retry
    - Test 9 passes: LightRAGReranker.compress_documents sorts by score, sets metadata
    - Test 10 passes: async method signature matches Protocol
  </behavior>
  <action>
    Create `src/lightrag_langchain/reranker.py`. Structure:

    **1. Imports section:**
    - `from __future__ import annotations`
    - `from typing import Any, Protocol` — for Reranker Protocol
    - `from collections.abc import Sequence` — for compress_documents signature
    - `import httpx` — async HTTP client
    - `from tenacity import retry, stop_after_attempt, wait_exponential, retry_if_exception_type` — retry decorator
    - `from langchain_core.documents import Document, BaseDocumentCompressor` — LangChain integration
    - `from lightrag_langchain.config import RerankerConfig` — config type

    **2. Module-level httpx logging suppression** (per RESEARCH.md Security Domain):
    - `import logging; logging.getLogger("httpx").setLevel(logging.WARNING)` — prevents API key leakage in debug logs

    **3. Reranker Protocol** (D-05):
    ```python
    class Reranker(Protocol):
        async def rerank(
            self, query: str, documents: list[str], top_n: int | None = None
        ) -> list[dict[str, Any]]: ...
    ```
    Returns list of `{"index": int, "relevance_score": float}`.

    **4. _post_rerank helper** — thin async HTTP POST with tenacity retry (D-07, D-08):
    - Decorated with `@retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, min=1, max=4), retry=retry_if_exception_type((httpx.HTTPStatusError, httpx.TransportError)))`
    - Inside: create `httpx.AsyncClient(timeout=30.0)`, post, check `response.status_code >= 500` → raise_for_status, return `response.json()`
    - 4xx errors propagate immediately (raised by httpx on response.raise_for_status(), not caught by retry filter)
    - Uses `async with httpx.AsyncClient(...) as client:` pattern

    **5. ali_rerank() adapter** — follows upstream LightRAG ali_rerank pattern (D-09):
    - Default base_url: `https://dashscope.aliyuncs.com/api/v1/services/rerank/text-rerank/text-rerank`
    - Request format: aliyun nested `{"model": ..., "input": {"query": ..., "documents": ...}, "parameters": {...}}`
    - Top_n goes in `payload["parameters"]["top_n"]`
    - Calls _post_rerank, extracts `response["output"]["results"]`
    - Normalized each result to `{"index": result["index"], "relevance_score": result["relevance_score"]}`
    - Signature: `async def ali_rerank(query, documents, model, base_url, api_key, top_n=None) -> list[dict]`

    **6. cohere_rerank() adapter** — follows upstream cohere_rerank pattern:
    - Default base_url: `https://api.cohere.com/v2/rerank`
    - Request format: standard `{"model": ..., "query": ..., "documents": ...}`
    - Calls _post_rerank, extracts `response["results"]`
    - Normalized same format

    **7. jina_rerank() adapter** — follows upstream jina_rerank pattern:
    - Default base_url: `https://api.jina.ai/v1/rerank`
    - Request format: standard (same as cohere)
    - Calls _post_rerank, extracts `response["results"]`
    - Normalized same format

    **8. create_reranker() factory** (D-05, D-06 raw layer):
    - Takes `config: RerankerConfig` parameter
    - Dispatches by `config.binding` string (case-insensitive):
      - "cohere" → returns `_CohereReranker` instance
      - "jina" → returns `_JinaReranker` instance
      - "aliyun" → returns `_AliyunReranker` instance (also accept "dashscope" as alias)
      - Any other value → raises `ValueError(f"Unknown reranker binding: {config.binding}")`
    - Each adapter class stores config as instance vars, implements `async rerank(self, query, documents, top_n=None)` that delegates to the thin adapter function
    - `__repr__` returns `f"Reranker(binding={self._binding}, model={self._model})"` — no api_key

    **9. LightRAGReranker(BaseDocumentCompressor)** (D-06 top layer):
    - Constructor: `__init__(self, reranker: Reranker, top_n: int | None = None)`
    - `compress_documents(self, documents: Sequence[Document], query: str, **kwargs) -> Sequence[Document]`:
      1. Extract `page_content` from each Document
      2. Call `asyncio.run(self._reranker.rerank(query, texts, self._top_n))` — sync wrapper around async
      3. Sort documents by relevance_score descending
      4. Write `relevance_score` to `document.metadata["relevance_score"]`
      5. Return sorted list
    - Also override `async acompress_documents(self, documents, query, **kwargs)`:
      - Call `await self._reranker.rerank(query, texts, self._top_n)` directly (truly async)
      - Sort and set metadata same as sync version

    **File targets:** ~150-200 lines. Per D-16, 4 files total, this is the largest at ~150-200 lines.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "class Reranker(Protocol)" src/lightrag_langchain/reranker.py` returns 1
    - Source assertion: `grep -c "def create_reranker" src/lightrag_langchain/reranker.py` returns 1
    - Source assertion: `grep -c "class LightRAGReranker" src/lightrag_langchain/reranker.py` returns 1
    - Source assertion: `grep -c "async def ali_rerank\|async def cohere_rerank\|async def jina_rerank" src/lightrag_langchain/reranker.py` — at least 3 adapter functions
    - Source assertion: `grep -c "BaseDocumentCompressor" src/lightrag_langchain/reranker.py` returns 1
    - Source assertion: `grep -c "from tenacity import" src/lightrag_langchain/reranker.py` returns 1
    - Source assertion: `grep -c "httpx" src/lightrag_langchain/reranker.py` >= 2
    - Source assertion: `grep -c "output.*results\|response\[.results.\]" src/lightrag_langchain/reranker.py` — response normalization for both aliyun and standard formats
    - Source assertion: `grep -c "get_secret_value\|_api_key" src/lightrag_langchain/reranker.py` — api_key access
    - Behavior: `import lightrag_langchain.reranker` succeeds without network
    - File line count: 150-250 lines
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_reranker.py -x -q</automated>
  </verify>
  <done>src/lightrag_langchain/reranker.py provides Reranker Protocol, 3 thin adapters, create_reranker() factory, and LightRAGReranker compressor. All 10 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config → reranker.py | RerankerConfig (including SecretStr api_key) passed to factory |
| reranker.py → external APIs | HTTP requests to aliyun/cohere/jina with Bearer token auth |
| reranker.py → LangChain | Documents compressed and scored, passed to ContextualCompressionRetriever |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | httpx logging | mitigate | `logging.getLogger("httpx").setLevel(logging.WARNING)` at module level. Authorization header not exposed in logs. |
| T-03-03-02 | Information Disclosure | adapter __repr__ | mitigate | __repr__ shows binding and model only, never api_key. |
| T-03-03-03 | Spoofing | configurable base_url | accept | base_url from .env is trusted source. User configures their own provider endpoints. httpx enforces TLS by default. |
| T-03-03-04 | Denial of Service | External API dependency | mitigate | httpx timeout (30s default). tenacity retry caps at 3 attempts with exponential backoff. 4xx errors fail fast (no retry). |
| T-03-03-05 | Tampering | External API response | mitigate | Response format validated — both aliyun (output.results) and standard (results) paths check isinstance(results, list). Empty results logged as warning. |
| T-03-03-SC | Tampering | httpx, tenacity dependencies | mitigate | Both packages passed slopcheck [OK] in RESEARCH.md. |
</threat_model>

<verification>
Full test suite for this module:
- `python -m pytest tests/test_reranker.py -x -q` — all 10 tests pass
- `python -c "import lightrag_langchain.reranker; print('import OK')"` — module imports without network
</verification>

<success_criteria>
1. `create_reranker()` dispatches to correct adapter by RERANK_BINDING value (LLM-03)
2. aliyun response format (`output.results[...]`) normalized to `[{index, relevance_score}]`
3. cohere/jina response format (`results[...]`) normalized to `[{index, relevance_score}]`
4. Unknown binding raises clear ValueError
5. HTTP calls retry on 5xx/transport errors with 3 attempts, exponential backoff (1s→2s→4s per D-08)
6. 4xx errors propagate immediately without retry
7. LightRAGReranker.compress_documents() extracts page_content, calls reranker, writes relevance_score to metadata, sorts descending by score
8. async acompress_documents() provides truly async path
9. No api_key leakage in logs, repr, or error messages (D-07, Claude's discretion)
</success_criteria>

<output>
Create `.planning/phases/03-llm-integration/03-03-SUMMARY.md` when done
</output>
