---
phase: 03-llm-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .env.example
  - src/lightrag_langchain/config.py
  - tests/conftest.py
autonomous: true
requirements:
  - D-13

must_haves:
  truths:
    - "KEYWORD_LANGUAGE is configurable via .env with default value 'Chinese'"
    - "langchain-openai, httpx, tenacity, tiktoken are declared as direct dependencies in pyproject.toml"
    - "Phase 3 test fixtures (mock_llm_config, mock_embedding_config, mock_reranker_config, mock_query_params_config) are available in conftest.py"
  artifacts:
    - path: "pyproject.toml"
      provides: "Phase 3 dependency declarations"
      contains: "langchain-openai"
    - path: ".env.example"
      provides: "KEYWORD_LANGUAGE env var documentation"
      contains: "KEYWORD_LANGUAGE"
    - path: "src/lightrag_langchain/config.py"
      provides: "QueryParamsConfig.keyword_language field"
      contains: "keyword_language"
    - path: "tests/conftest.py"
      provides: "Phase 3 test fixtures"
      contains: "mock_llm_config"
  key_links:
    - from: "QueryParamsConfig.keyword_language"
      to: ".env KEYWORD_LANGUAGE"
      via: "pydantic-settings env var auto-binding"
      pattern: "keyword_language.*str.*="
    - from: "tests/test_llm.py (Plan 03-02)"
      to: "tests/conftest.py mock_llm_config"
      via: "pytest fixture name matching"
      pattern: "mock_llm_config"
---

<objective>
Prepare project infrastructure for Phase 3 by declaring new direct dependencies, adding the KEYWORD_LANGUAGE configuration variable, and creating shared pytest fixtures for LLM/embedding/reranker/token-budget tests.

Purpose: Phase 3 introduces 4 new source modules (llm.py, reranker.py, keywords.py, token_budget.py) that depend on 4 new libraries (langchain-openai, httpx, tenacity, tiktoken) and a new configuration variable (KEYWORD_LANGUAGE). This plan establishes the dependency declarations, config extension, and test infrastructure before the module implementation plans run in parallel.

Output: Updated pyproject.toml with 4 new dependency entries, .env.example with KEYWORD_LANGUAGE, config.py with keyword_language field on QueryParamsConfig, and conftest.py with 4 fixture factories for Phase 3 test modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-llm-integration/03-RESEARCH.md

# Current implementation files to extend
@src/lightrag_langchain/config.py
@pyproject.toml
@.env.example
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Declare Phase 3 direct dependencies in pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>pyproject.toml</read_first>
  <action>
    Add four new entries to the `dependencies` list in pyproject.toml under `[project]`:
    - "langchain-openai>=1.2,&lt;2.0" — ChatOpenAI, OpenAIEmbeddings, with_structured_output for keyword extraction
    - "httpx>=0.28,&lt;1.0" — async HTTP client for reranker API calls to aliyun/cohere/jina (replaces upstream aiohttp per D-07)
    - "tenacity>=9.0,&lt;10.0" — retry decorator for reranker HTTP calls (consistent with Phase 2 pool.py pattern per D-08)
    - "tiktoken>=0.7,&lt;1.0" — BPE tokenizer for token budget calculation (matches upstream LightRAG TiktokenTokenizer per D-18)

    Insert these four entries after the existing "pydantic-settings>=2.14,&lt;3.0" entry, maintaining alphabetical order within the dependencies list. The existing dependencies (asyncpg, pgvector, pydantic, pydantic-settings) must remain unchanged.

    All four packages are already installed in the environment (verified by RESEARCH.md Section "Standard Stack"). This change only declares them in pyproject.toml so they become explicit direct dependencies rather than transitive.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "langchain-openai" pyproject.toml` returns 1
    - Source assertion: `grep -c "httpx" pyproject.toml` returns 1
    - Source assertion: `grep -c "tenacity" pyproject.toml` returns 1
    - Source assertion: `grep -c "tiktoken" pyproject.toml` returns 1
    - Source assertion: All four new entries are in the `[project] dependencies` list (not under `[project.optional-dependencies]`)
    - Source assertion: `grep -c "asyncpg" pyproject.toml` remains 1 (existing deps preserved)
  </acceptance_criteria>
  <verify>
    <automated>grep -E "langchain-openai|httpx|tenacity|tiktoken" pyproject.toml | wc -l | xargs test 4 -eq</automated>
  </verify>
  <done>pyproject.toml dependencies list contains langchain-openai, httpx, tenacity, and tiktoken with version bounds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add KEYWORD_LANGUAGE env var and config field</name>
  <files>.env.example, src/lightrag_langchain/config.py</files>
  <read_first>.env.example, src/lightrag_langchain/config.py</read_first>
  <action>
    Per D-13: the keyword extraction prompt uses a `{language}` placeholder that must be configurable via .env with a default of "Chinese".

    **In .env.example:**
    Add a new section after the Query Parameters block (after line 45, `KG_CHUNK_PICK_METHOD=VECTOR`), with a blank line separator:
    ```
    # Keyword Extraction (LLM-04, defaults shown)
    KEYWORD_LANGUAGE=Chinese
    ```
    No trailing new section header needed — this is a single-line addition under Query Parameters context.

    **In config.py:** Add a field to `QueryParamsConfig` (the model at lines 129-155):
    - Field: `keyword_language: str = "Chinese"` — inserted after `kg_chunk_pick_method` (line 144) and before the `@model_validator` (line 146).
    - No additional validation needed — it's a simple string passthrough to the prompt template.
    - No changes to the existing `check_token_budget` validator.

    The env var name `KEYWORD_LANGUAGE` maps to `query_params.keyword_language` via pydantic-settings' nested model default behavior (Settings uses `env_nested_delimiter="__"`, so the full env path would be `QUERY_PARAMS__KEYWORD_LANGUAGE`). However, since the default is "Chinese" and most users won't change it, the simple env var name is fine for documentation in .env.example. Note: pydantic-settings with `nested_model_default_partial_update=True` means the env var `KEYWORD_LANGUAGE` is NOT auto-bound to the nested field. The .env.example entry documents the intent; the actual env var that pydantic-settings reads is `QUERY_PARAMS__KEYWORD_LANGUAGE=Chinese`. Use that exact nested form in .env.example to ensure correct auto-binding.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "KEYWORD_LANGUAGE" .env.example` returns 1
    - Source assertion: `.env.example` line containing KEYWORD_LANGUAGE includes value "Chinese"
    - Source assertion: `grep -c "keyword_language" src/lightrag_langchain/config.py` returns 1
    - Source assertion: keyword_language field has type annotation `str` and default `"Chinese"`
    - Behavior assertion: `from lightrag_langchain.config import QueryParamsConfig; q = QueryParamsConfig(); assert q.keyword_language == "Chinese"` (via Python)
    - CLI assertion: `python -c "from lightrag_langchain.config import QueryParamsConfig; q = QueryParamsConfig(); print(q.keyword_language)"` prints "Chinese"
  </acceptance_criteria>
  <verify>
    <automated>python -c "from lightrag_langchain.config import QueryParamsConfig; q = QueryParamsConfig(); assert q.keyword_language == 'Chinese'; print('OK')"</automated>
  </verify>
  <done>KEYWORD_LANGUAGE is documented in .env.example with default "Chinese", and QueryParamsConfig has keyword_language: str = "Chinese" field.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend conftest.py with Phase 3 test fixtures</name>
  <files>tests/conftest.py</files>
  <read_first>tests/conftest.py, src/lightrag_langchain/config.py</read_first>
  <action>
    Add four pytest fixture factories to tests/conftest.py for Phase 3 test modules. Each fixture uses the existing `temp_env_file` pattern to create a temporary .env with specific config values, then returns the appropriate config object.

    **Fixture 1: `mock_llm_config`** — returns a `LlmConfig` instance with test values:
    - binding="test_llm", binding_host="https://test-llm.example.com/v1", binding_api_key=SecretStr("test-llm-key"), model="test-model", temperature=0.0, max_tokens=4096
    - Uses `temp_env_file` to write: LLM_BINDING=test_llm, LLM_BINDING_HOST=https://test-llm.example.com/v1, LLM_BINDING_API_KEY=test-llm-key, LLM_MODEL=test-model
    - Returns `LlmConfig(binding="test_llm", binding_host="https://test-llm.example.com/v1", binding_api_key=SecretStr("test-llm-key"), model="test-model")` — constructed directly (not from Settings), so tests don't need a full .env

    **Fixture 2: `mock_embedding_config`** — returns an `EmbeddingConfig` instance:
    - binding="test_emb", binding_host="https://test-emb.example.com/v1", binding_api_key=SecretStr("test-emb-key"), model="test-emb-model", dim=1024

    **Fixture 3: `mock_reranker_config`** — returns a `RerankerConfig` instance:
    - binding="cohere", binding_host="https://api.cohere.com/v2/rerank", binding_api_key=SecretStr("test-rerank-key"), model="rerank-v3.5", min_rerank_score=0.0

    **Fixture 4: `mock_query_params_config`** — returns a `QueryParamsConfig` instance:
    - Uses defaults plus keyword_language="Chinese"
    - Constructed as `QueryParamsConfig(max_entity_tokens=4000, max_relation_tokens=5000, max_total_tokens=20000, keyword_language="Chinese")`
    - Token budget invariant: 4000 + 5000 = 9000 < 20000 ✓

    Also add a **Fixture 5: `mock_httpx_client`** that returns an `AsyncMock` wrapping `httpx.AsyncClient`:
    - ```python
      @pytest.fixture
      def mock_httpx_client():
          from unittest.mock import AsyncMock, MagicMock
          client = AsyncMock()
          client.post = AsyncMock()
          return client
      ```

    All fixtures are constructed directly via model constructors (not through Settings singleton), so they work without a real .env file. Patterns follow existing conftest.py fixtures: module docstring, `from __future__ import annotations`, explicit imports, type annotations. The `LlmConfig`, `EmbeddingConfig`, `RerankerConfig`, `QueryParamsConfig` classes are imported from `lightrag_langchain.config`; `SecretStr` from `pydantic`.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "mock_llm_config" tests/conftest.py` returns 1
    - Source assertion: `grep -c "mock_embedding_config" tests/conftest.py` returns 1
    - Source assertion: `grep -c "mock_reranker_config" tests/conftest.py` returns 1
    - Source assertion: `grep -c "mock_query_params_config" tests/conftest.py` returns 1
    - Source assertion: `grep -c "mock_httpx_client" tests/conftest.py` returns 1
    - CLI assertion: `python -m pytest tests/conftest.py --co -q 2>&1 | grep "mock_llm_config"` — pytest discovers all 5 fixtures
    - No existing fixtures (temp_env_file, mock_pool, mock_conn) are removed or altered
    - Fixtures are importable: `python -c "from tests.conftest import mock_llm_config; print(type(mock_llm_config))"` does not error
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/conftest.py --co -q 2>&1 | grep -E "mock_llm_config|mock_embedding_config|mock_reranker_config|mock_query_params_config|mock_httpx_client" | wc -l | xargs test 5 -eq</automated>
  </verify>
  <done>tests/conftest.py provides 5 Phase 3 fixtures discoverable by pytest. Existing fixtures unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env file → config.py | Configuration values cross from filesystem into application memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | .env.example | accept | This file is a committed template with placeholder values — no real secrets. Accepting as documentation artifact. |
| T-03-01-02 | Tampering | config.py QueryParamsConfig | mitigate | keyword_language field is Pydantic-validated string with default "Chinese". No injection vector — it's only used as a string placeholder in prompt templates, never executed or eval'd. |
| T-03-01-SC | Tampering | pip install dependencies | mitigate | All 4 packages passed slopcheck [OK] in RESEARCH.md Package Legitimacy Audit. All are well-established (3+ years, 20M+ monthly downloads). |
</threat_model>

<verification>
Run full existing test suite to confirm no regressions from config.py and conftest.py changes:
- `python -m pytest tests/ -x -q` — all existing tests must pass
</verification>

<success_criteria>
1. `pyproject.toml` contains 4 new dependency entries (langchain-openai, httpx, tenacity, tiktoken) with version bounds
2. `.env.example` documents KEYWORD_LANGUAGE=Chinese
3. `QueryParamsConfig` has `keyword_language: str = "Chinese"` field, instantiable with default
4. `tests/conftest.py` exposes `mock_llm_config`, `mock_embedding_config`, `mock_reranker_config`, `mock_query_params_config`, `mock_httpx_client` fixtures
5. Full existing test suite passes after all changes
</success_criteria>

<output>
Create `.planning/phases/03-llm-integration/03-01-SUMMARY.md` when done
</output>
