---
phase: 06-qa-chain
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - tests/test_chain_base.py
  - tests/test_chain_keywords.py
  - tests/test_chain_dispatch.py
  - tests/test_chain_stream.py
autonomous: true
requirements:
  - CHAIN-01
  - CHAIN-02
  - CHAIN-03

must_haves:
  truths:
    - "Full chain pipeline tests verify invoke/ainvoke return correct dict structure (CHAIN-01, CHAIN-02)"
    - "Keyword resolution tests verify pre-provided keywords skip LLM extraction (CHAIN-03)"
    - "Subclass dispatch tests verify all 6 Chain classes have correct mode and template selection"
    - "Streaming tests verify astream yields str tokens then final dict (D-09, D-10)"
    - "Reference list tests verify dedup by file_path with sequential integer IDs (D-11, D-12)"
  artifacts:
    - path: "tests/test_chain_base.py"
      provides: "Core pipeline integration tests"
      min_lines: 200
    - path: "tests/test_chain_keywords.py"
      provides: "CHAIN-03 keyword resolution tests"
      min_lines: 100
    - path: "tests/test_chain_dispatch.py"
      provides: "6 subclass dispatch + mode tests"
      min_lines: 120
    - path: "tests/test_chain_stream.py"
      provides: "astream token + dict contract tests"
      min_lines: 100
  key_links:
    - from: "tests/test_chain_base.py"
      to: "chain/base.py LightRAGBaseChain"
      via: "from lightrag_langchain.chain.base import LightRAGBaseChain"
      pattern: "LightRAGBaseChain\\("
    - from: "tests/test_chain_stream.py"
      to: "chain/base.py LightRAGBaseChain.astream"
      via: "async for chunk in chain.astream(query)"
      pattern: "\\.astream\\("
    - from: "tests/test_chain_dispatch.py"
      to: "chain/chains.py (6 subclasses)"
      via: "from lightrag_langchain.chain.chains import NaiveChain, ..."
      pattern: "NaiveChain\\("
---

<objective>
Create the complete test suite for Phase 6: core pipeline integration, keyword resolution (CHAIN-03), subclass dispatch, and streaming contract verification (D-09/D-10). All tests use mock fixtures from conftest.py — no real LLM or database required.

Purpose: Verify every requirement (CHAIN-01/02/03), every decision (D-07 through D-12), and every threat-mitigation claim from the PLAN.md threat models. Tests are the phase gate before `/gsd-verify-work`.

Output: 4 test files: `test_chain_base.py` (core pipeline), `test_chain_keywords.py` (CHAIN-03), `test_chain_dispatch.py` (6 subclasses), `test_chain_stream.py` (D-09/D-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-qa-chain/06-RESEARCH.md
@.planning/phases/06-qa-chain/06-PATTERNS.md

# Source code under test
@src/lightrag_langchain/chain/base.py
@src/lightrag_langchain/chain/chains.py

# Test fixture definitions
@tests/conftest.py

# Analog: class-per-test-group pattern, AsyncMock usage, @pytest.mark.asyncio
@tests/test_retriever.py
@tests/test_keywords.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_chain_base.py — core pipeline integration tests</name>
  <files>tests/test_chain_base.py</files>
  <read_first>
- tests/test_retriever.py (full file, lines 1-219) — source of truth for: class-per-test-group pattern, fixture injection, AsyncMock patterns, _make_* factory functions, json.loads for Document validation, docstring style
- tests/test_keywords.py (lines 105-143) — pattern for mocking llm.with_structured_output (MagicMock returning structured_mock with AsyncMock ainvoke)
- tests/conftest.py — mock_llm, mock_retriever, make_entity_doc, make_chunk_doc fixture definitions
- src/lightrag_langchain/chain/base.py — LightRAGBaseChain class definition, all method signatures
  </read_first>
  <action>
Create `tests/test_chain_base.py`. Module docstring: "Integration tests for LightRAGBaseChain core pipeline: invoke/ainvoke dict structure, empty results, template selection, system prompt override, reference list generation, and token budget ordering."

Imports:
- `from __future__ import annotations`
- `import json`, `import pytest`
- `from unittest.mock import AsyncMock, MagicMock, call, patch`
- `from langchain_core.documents import Document`
- `from lightrag_langchain.chain.base import LightRAGBaseChain`
- `from lightrag_langchain.chain.chains import NaiveChain, BypassChain`
- `from lightrag_langchain.keywords import KeywordsSchema`

Implement test classes following the retriever test pattern (one class per feature group, descriptive docstrings per test method).

**Class 1: `TestChainInvoke` — invoke() dict structure and pipeline verification**

Test 1: `test_invoke_returns_dict_structure(mock_llm, mock_retriever, make_chunk_doc)`
- Setup: chunk_doc = make_chunk_doc(content="test chunk"), set mock_retriever.ainvoke.return_value = [chunk_doc]. Set mock_llm.ainvoke mock_response.content = "Answer text".
- Execute: chain = NaiveChain(retriever=mock_retriever, llm=mock_llm), result = chain.invoke("test query", hl_keywords=["kw"], ll_keywords=["kw"])
- Assert: result["answer"] == "Answer text", result["mode"] == "naive", isinstance(result["sources"], list), isinstance(result["keywords"], dict), result["keywords"] == {"high_level": ["kw"], "low_level": ["kw"]}

Test 2: `test_invoke_async_bridge(mock_llm, mock_retriever, make_chunk_doc)`
- Setup: same as Test 1. Then verify invoke() is synchronous (returns dict, not coroutine).
- Execute: same, assert isinstance(result, dict)

**Class 2: `TestChainAinvoke` — async pipeline verification**

Test 3: `@pytest.mark.asyncio` `test_ainvoke_pipeline_order(mock_llm, mock_retriever, make_chunk_doc)`
- Setup: chunk_doc = make_chunk_doc(). Set mock_retriever.ainvoke.return_value = [chunk_doc]. Set mock_llm.ainvoke with mock_response.content = "Answer".
- Also set: mock_llm.with_structured_output = MagicMock() returning a structured_mock with structured_mock.ainvoke = AsyncMock(return_value=KeywordsSchema(high_level_keywords=["hw"], low_level_keywords=["lw"]))
- Execute: chain = NaiveChain(retriever=mock_retriever, llm=mock_llm), result = await chain.ainvoke("test query")
- Assert: result["answer"] == "Answer", result["keywords"] == {"high_level": ["hw"], "low_level": ["lw"]}, result["mode"] == "naive"
- Verify call order: mock_retriever.ainvoke.assert_awaited_once(), mock_llm.ainvoke.assert_awaited_once()

**Class 3: `TestEmptyResults` — empty retriever results (Claude's Discretion)**

Test 4: `test_empty_retriever_still_calls_llm(mock_llm, mock_retriever)`
- Setup: mock_retriever.ainvoke.return_value = []. mock_llm.ainvoke mock_response.content = "I don't have enough information".
- Execute: chain = NaiveChain(retriever=mock_retriever, llm=mock_llm), result = chain.invoke("test query", hl_keywords=["kw"], ll_keywords=["kw"])
- Assert: result["answer"] == "I don't have enough information", result["sources"] == [], mock_llm.ainvoke.assert_awaited_once() — chain does NOT short-circuit on empty results; LLM receives empty context per Claude's Discretion

**Class 4: `TestTemplateSelection` — D-07 template selection per mode**

Test 5: `test_naive_mode_uses_naive_templates(mock_llm, mock_retriever, make_chunk_doc)`
- Setup: chunk_doc = make_chunk_doc(content="chunk text"). mock_retriever.ainvoke.return_value = [chunk_doc]. mock_llm.ainvoke mock_response.content = "Answer".
- Execute: chain = NaiveChain(retriever=mock_retriever, llm=mock_llm), result = chain.invoke("q", hl_keywords=["h"], ll_keywords=["l"])
- Assert: mock_llm.ainvoke was called. Extract the SystemMessage content from call_args. Assert "Document Chunks" is in the system prompt (naive template talks about Document Chunks, not Knowledge Graph Data). The naive template uses `{content_data}` placeholder successfully formatted.

Test 6: `test_kg_mode_uses_kg_templates(mock_llm, mock_retriever, make_chunk_doc)`
- Setup: same but use LocalChain (import from lightrag_langchain.chain.chains). Set chunk_doc metadata["retrieval_mode"] = "local" and document_type = "entity" for the make_entity_doc factory.
- Actually, use make_entity_doc fixture. Add it to imports and fixture params.
- Execute: chain = LocalChain(retriever=mock_retriever, llm=mock_llm), result = chain.invoke("q", hl_keywords=["h"], ll_keywords=["l"])
- Assert: System prompt contains "Knowledge Graph Data" (KG template references both entities and relations).

**Class 5: `TestSystemPromptOverride` — D-08 full override**

Test 7: `test_system_prompt_override_replaces_entire_prompt(mock_llm, mock_retriever, make_chunk_doc)`
- Setup: chunk_doc = make_chunk_doc(). mock_retriever.ainvoke.return_value = [chunk_doc]. mock_llm.ainvoke mock_response.content = "Answer".
- Execute: chain = NaiveChain(retriever=mock_retriever, llm=mock_llm), result = chain.invoke("q", system_prompt="CUSTOM SYSTEM PROMPT", hl_keywords=["h"], ll_keywords=["l"])
- Assert: Extract SystemMessage content from mock_llm.ainvoke.call_args. Assert it equals "CUSTOM SYSTEM PROMPT" exactly — no template wrapping, no formatting, no appended user_prompt. D-08: full override, no partial coverage.

**Class 6: `TestReferenceList` — D-11/D-12 reference list generation**

Test 8: `test_reference_list_dedup_by_file_path(mock_llm, mock_retriever, make_chunk_doc)`
- Setup: Create 3 chunk docs with file_paths ["a.txt", "b.txt", "a.txt"] (a.txt duplicated). mock_retriever.ainvoke.return_value = [chunk_doc_a1, chunk_doc_b, chunk_doc_a2]. mock_llm.ainvoke content = "Answer".
- Execute: chain = NaiveChain(retriever=mock_retriever, llm=mock_llm), result = chain.invoke("q", hl_keywords=["h"], ll_keywords=["l"])
- Assert: result["sources"] has 2 entries (a.txt and b.txt, not 3), reference_id are integers (1, 2), file_path values match. Assert result["sources"][0]["reference_id"] == 1 and isinstance int.

Test 9: `test_reference_list_excludes_unknown_source(mock_llm, mock_retriever, make_chunk_doc)`
- Setup: Create chunk docs with file_paths ["unknown_source", "valid.txt"]. mock_retriever.ainvoke.return_value = [chunk_unknown, chunk_valid].
- Execute: chain = NaiveChain(retriever=mock_retriever, llm=mock_llm), result = chain.invoke("q", hl_keywords=["h"], ll_keywords=["l"])
- Assert: result["sources"] has 1 entry (only valid.txt). "unknown_source" is filtered out per D-11.

**Class 7: `TestTokenBudget` — token budget application ordering**

Test 10: `test_token_budget_called(mock_llm, mock_retriever, make_entity_doc, make_chunk_doc)`
- Setup: Create many entity docs (20+) so token budget truncation is exercised. mock_retriever.ainvoke.return_value = [make_entity_doc(entity_name=f"e{i}") for i in range(30)] + [make_chunk_doc()]. mock_llm.ainvoke content = "Answer".
- Execute: chain = LocalChain(retriever=mock_retriever, llm=mock_llm), result = chain.invoke("q", hl_keywords=["h"], ll_keywords=["l"])
- Assert: result["answer"] is a string (not empty). The chain did not crash on token budget computation. With mock_query_params_config fixture (max_entity_tokens=4000, max_total_tokens=20000), the entity list should be truncated.

**Module-level helper if needed:**
Define `_naive_chain(mock_retriever, mock_llm)` factory function to reduce duplication, OR just inline construction per test (preferred for readability).

**Test pattern rules:**
- All async tests must be decorated with `@pytest.mark.asyncio`
- Mock LLM's `with_structured_output` only needs to be set up in tests that call chain WITHOUT pre-provided keywords (tests that test keyword extraction)
- Tests with `hl_keywords` and `ll_keywords` arguments do NOT need `with_structured_output` setup (CHAIN-03: extraction is skipped)
- Use `mock_llm.ainvoke.assert_awaited_once()` to verify LLM was called
- Use `mock_retriever.ainvoke.assert_awaited_once()` to verify retriever was called
- System prompt content can be verified via `mock_llm.ainvoke.call_args[0][0]` which is the messages list; extract `[0].content` for the SystemMessage
  </action>
  <verify>
    <automated>python -m pytest tests/test_chain_base.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
10+ tests across 7 test classes covering: invoke dict structure, ainvoke pipeline ordering, empty results behavior, template selection (naive vs KG), system_prompt override (D-08), reference list dedup + integer IDs (D-11/D-12), and token budget application. All tests pass with mock fixtures only — no real LLM or database.
  </done>
  <acceptance_criteria>
- `tests/test_chain_base.py` exists with tests passing: `pytest tests/test_chain_base.py -x -v` exits 0
- `TestChainInvoke` verifies invoke returns dict with answer, sources, keywords, mode keys
- `TestChainAinvoke` verifies async pipeline calls retriever then LLM in order
- `TestEmptyResults` verifies empty retriever results still call LLM (no shortcut)
- `TestTemplateSelection` verifies naive mode uses naive templates, KG modes use KG templates
- `TestSystemPromptOverride` verifies D-08 full override replaces entire prompt without template wrapping
- `TestReferenceList` verifies file_path dedup (D-11) and integer reference_id (D-12)
- `TestTokenBudget` verifies token budget computation does not crash with large entity lists
- All tests use only conftest.py fixtures — no real .env, LLM, or database
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_chain_keywords.py + tests/test_chain_dispatch.py — CHAIN-03 + subclass tests</name>
  <files>
    - tests/test_chain_keywords.py
    - tests/test_chain_dispatch.py
  </files>
  <read_first>
- tests/test_keywords.py (full file) — pattern for CHAIN-03 keyword tests: mock with_structured_output, verify LLM call/not-called
- src/lightrag_langchain/chain/chains.py — all 6 subclass class names and mode values
- src/lightrag_langchain/chain/base.py — _resolve_keywords method (CHAIN-03 logic), _build_system_prompt (D-07 template selection)
- tests/conftest.py — mock_llm fixture (know that with_structured_output defaults to MagicMock on AsyncMock)
  </read_first>
  <action>
**Part A: Create `tests/test_chain_keywords.py`**

Module docstring: "Unit tests for CHAIN-03: pre-provided keywords bypass LLM keyword extraction. Tests verify that when hl_keywords and ll_keywords are both provided, extract_keywords() is never called."

Imports: same pattern as test_chain_base.py (from __future__, pytest, AsyncMock, MagicMock, chain imports).

**Class: `TestKeywordResolution`**

Test 1: `test_pre_provided_keywords_skip_llm_extraction(mock_llm, mock_retriever)`
- Setup: mock_retriever.ainvoke.return_value = []. mock_llm.ainvoke mock_response.content = "Answer". DO NOT set up with_structured_output on mock_llm.
- Execute: chain = NaiveChain(retriever=mock_retriever, llm=mock_llm), result = chain.invoke("test query", hl_keywords=["high"], ll_keywords=["low"])
- Assert: result["keywords"] == {"high_level": ["high"], "low_level": ["low"]}. Verify mock_llm.ainvoke.call_count == 1 (only called once for final answer, NOT for keyword extraction). Verify mock_llm.with_structured_output was NOT called (if mock_llm is AsyncMock, check assert_not_called).

Test 2: `test_no_keywords_triggers_llm_extraction(mock_llm, mock_retriever)`
- Setup: mock_retriever.ainvoke.return_value = []. Set up with_structured_output: structured_mock = AsyncMock(); structured_mock.ainvoke = AsyncMock(return_value=KeywordsSchema(high_level_keywords=["extracted_hl"], low_level_keywords=["extracted_ll"])); mock_llm.with_structured_output = MagicMock(return_value=structured_mock). Set mock_llm.ainvoke mock_response.content = "Answer after extraction".
- Execute: chain = NaiveChain(retriever=mock_retriever, llm=mock_llm), result = chain.invoke("test query") — NO hl_keywords/ll_keywords args
- Assert: result["keywords"] == {"high_level": ["extracted_hl"], "low_level": ["extracted_ll"]}. Verify mock_llm.with_structured_output was called (keyword extraction happened). Verify result["answer"] == "Answer after extraction".

Test 3: `test_only_hl_keywords_provided_triggers_extraction(mock_llm, mock_retriever)`
- Setup: Set up with_structured_output same as Test 2. mock_retriever.ainvoke.return_value = [].
- Execute: chain = NaiveChain(retriever=mock_retriever, llm=mock_llm), result = chain.invoke("test query", hl_keywords=["high"]) — ONLY hl_keywords, ll_keywords is None
- Assert: Since BOTH must be provided to skip extraction (CHAIN-03), LLM extraction should still be called. Verify result["keywords"]["high_level"] is from extraction, not ["high"]. Verify with_structured_output was called.

Test 4: `test_bypass_chain_skips_keywords_entirely(mock_llm, mock_retriever)`
- Setup: mock_llm.ainvoke mock_response.content = "Bypass answer". DO NOT set with_structured_output.
- Execute: chain = BypassChain(retriever=mock_retriever, llm=mock_llm), result = chain.invoke("test query")
- Assert: result["keywords"] == {"high_level": [], "low_level": []} (BypassChain always returns empty keywords). mock_llm.with_structured_output was never called. mock_retriever.ainvoke was never called.

**Part B: Create `tests/test_chain_dispatch.py`**

Module docstring: "Unit tests for 6 Chain subclass dispatch: mode verification, template selection cross-check, and BypassChain special-case behavior."

Imports: same pattern, import all 6 chain subclasses.

**Class: `TestChainModes` — mode attribute verification**

Test 5: Parameterized test (or 6 individual tests) verifying each subclass mode:
- NaiveChain mode == "naive"
- LocalChain mode == "local"
- GlobalChain mode == "global"
- HybridChain mode == "hybrid"
- MixChain mode == "mix"
- BypassChain mode == "bypass"

Use `@pytest.mark.parametrize`:
```python
@pytest.mark.parametrize("chain_cls,expected_mode", [
    (NaiveChain, "naive"),
    (LocalChain, "local"),
    (GlobalChain, "global"),
    (HybridChain, "hybrid"),
    (MixChain, "mix"),
    (BypassChain, "bypass"),
])
def test_chain_mode(chain_cls, expected_mode, mock_llm, mock_retriever):
    chain = chain_cls(retriever=mock_retriever, llm=mock_llm)
    assert chain.mode == expected_mode
```

**Class: `TestBypassChain` — bypass-specific behavior**

Test 6: `test_bypass_skips_retrieval_and_keywords(mock_llm, mock_retriever)`
- Setup: mock_llm.ainvoke mock_response.content = "Bypass".
- Execute: chain = BypassChain(retriever=mock_retriever, llm=mock_llm), result = chain.invoke("q")
- Assert: result["mode"] == "bypass", result["sources"] == [], mock_retriever.ainvoke.assert_not_called(), result["keywords"] == {"high_level": [], "low_level": []}

Test 7: `test_bypass_uses_rag_response_with_empty_context(mock_llm, mock_retriever)`
- Setup: mock_llm.ainvoke mock_response.content = "Bypass".
- Execute: BypassChain(retriever=mock_retriever, llm=mock_llm).invoke("q")
- Assert: Extract SystemMessage from mock_llm.ainvoke.call_args[0][0][0]. Assert "context_data" is formatted as empty (the prompt should not contain retrieved data). The LLM receives RAG_RESPONSE_PROMPT with empty context.

**Class: `TestTemplateSelectionCrossCheck` — template dispatch verification**

Test 8: `test_naive_vs_kg_system_prompt_difference(mock_llm, mock_retriever, make_entity_doc, make_chunk_doc)`
- Setup: entity_doc = make_entity_doc(). mock_retriever.ainvoke.return_value = [entity_doc]. mock_llm.ainvoke mock_response.content = "Answer".
- Execute 1: NaiveChain(retriever=mock_retriever, llm=mock_llm).invoke("q", hl_keywords=["h"], ll_keywords=["l"])
- Capture sys_prompt_naive = SystemMessage content from mock_llm.ainvoke.call_args
- Reset mock_llm.ainvoke
- Execute 2: LocalChain(retriever=mock_retriever, llm=mock_llm).invoke("q", hl_keywords=["h"], ll_keywords=["l"])
- Capture sys_prompt_local = SystemMessage content
- Assert: sys_prompt_naive != sys_prompt_local (different templates used). Naive uses {content_data}, Local uses {context_data}. Naive mentions "Document Chunks" without "Knowledge Graph Data".
  </action>
  <verify>
    <automated>python -m pytest tests/test_chain_keywords.py tests/test_chain_dispatch.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
CHAIN-03 verified: pre-provided keywords skip LLM extraction (call_count == 1), partial keywords trigger extraction, BypassChain never calls keyword extraction. All 6 subclass modes verified via parametrized test. BypassChain special behavior confirmed (skips retriever + keywords). Template selection cross-check confirms naive vs KG template difference.
  </done>
  <acceptance_criteria>
- `tests/test_chain_keywords.py` exists with 4 tests: CHAIN-03 pre-provided skip, LLM extraction when missing, partial keywords trigger extraction, BypassChain never extracts
- `tests/test_chain_dispatch.py` exists with tests for all 6 subclass modes + BypassChain special behavior + template cross-check
- Both files pass: `pytest tests/test_chain_keywords.py tests/test_chain_dispatch.py -x -v` exits 0
- All tests use only conftest.py fixtures — no real .env, LLM, or database
- `@pytest.mark.asyncio` on all async test methods
- Parametrized mode test covers all 6 subclasses
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create tests/test_chain_stream.py — astream contract tests (D-09, D-10)</name>
  <files>tests/test_chain_stream.py</files>
  <read_first>
- tests/test_chain_base.py (from Task 1) — pattern for mock_llm setup, chain construction
- src/lightrag_langchain/chain/base.py — astream() method signature: `async def astream(self, query, *, system_prompt=None, hl_keywords=None, ll_keywords=None, **kwargs) -> AsyncIterator[str | dict]`
- src/lightrag_langchain/chain/chains.py — BypassChain.astream override (returns different final dict)
- tests/conftest.py — mock_llm fixture (astream side_effect pattern)
  </read_first>
  <action>
Create `tests/test_chain_stream.py`. Module docstring: "Unit tests for D-09 astream contract: yields str tokens then final dict. D-10: sources/keywords determined before LLM streaming begins."

Imports:
- `from __future__ import annotations`
- `import pytest`
- `from unittest.mock import AsyncMock, MagicMock, call`
- `from lightrag_langchain.chain.chains import NaiveChain, BypassChain`

**Class: `TestChainAstream`**

Test 1: `@pytest.mark.asyncio` `test_astream_yields_tokens_then_dict(mock_llm, mock_retriever)`
- Setup: mock_retriever.ainvoke.return_value = [] (empty result, simplest case).
- Set up mock_llm.astream to yield 3 token chunks:
  ```python
  async def _mock_stream(messages):
      yield MagicMock(content="Hello")
      yield MagicMock(content=" ")
      yield MagicMock(content="World")
  mock_llm.astream = MagicMock(side_effect=_mock_stream)
  ```
- Execute: chain = BypassChain(retriever=mock_retriever, llm=mock_llm). Collect all chunks via `async for chunk in chain.astream("test query"): chunks.append(chunk)`.
- Assert: len(chunks) == 4 (3 str tokens + 1 dict). chunks[0] == "Hello" (type str), chunks[1] == " " (type str), chunks[2] == "World" (type str). isinstance(chunks[3], dict). chunks[3]["answer"] == "Hello World". chunks[3]["mode"] == "bypass". chunks[3]["sources"] == [].

Test 2: `@pytest.mark.asyncio` `test_astream_final_dict_has_answer_sources_keywords_mode(mock_llm, mock_retriever)`
- Setup: same as Test 1 but with mock_retriever returning some docs so we verify sources in final dict.
- If using NaiveChain: need make_chunk_doc. Set up mock_llm.astream to yield one token.
- Execute: NaiveChain with hl_keywords=["h"], ll_keywords=["l"].
- Assert: final_dict is a dict with all 4 required keys: "answer" (non-empty str), "sources" (list), "keywords" (dict with high_level/low_level), "mode" (str == "naive").

Test 3: `@pytest.mark.asyncio` `test_sources_determined_before_streaming(mock_llm, mock_retriever, make_chunk_doc)` (D-10)
- Setup: chunk_doc = make_chunk_doc(file_path="unique/file.txt"). mock_retriever.ainvoke.return_value = [chunk_doc].
- Set up mock_llm.astream to yield single token: `async def _mock_stream(messages): yield MagicMock(content="T")`. mock_llm.astream = MagicMock(side_effect=_mock_stream).
- Execute: chain = NaiveChain(retriever=mock_retriever, llm=mock_llm). Call astream("q", hl_keywords=["h"], ll_keywords=["l"]).
- Assert: After collecting all chunks, verify the final dict has sources = [{"reference_id": 1, "file_path": "unique/file.txt"}]. The reference list was computed BEFORE the first token was yielded (since the last chunk carries the pre-computed data). D-10 verified: sources ready before LLM.

Test 4: `@pytest.mark.asyncio` `test_astream_empty_llm_output_yields_empty_answer(mock_llm, mock_retriever)`
- Setup: mock_retriever.ainvoke.return_value = []. Set up mock_llm.astream to be an empty async generator (yields nothing).
  ```python
  async def _empty_stream(messages):
      return
      yield  # unreachable, makes it async generator
  mock_llm.astream = MagicMock(side_effect=_empty_stream)
  ```
- Execute: chain = BypassChain(retriever=mock_retriever, llm=mock_llm). Collect chunks.
- Assert: Only 1 chunk (the final dict). final_dict["answer"] == "" (empty string — LLM generated no tokens).

Test 5: `@pytest.mark.asyncio` `test_astream_with_pre_provided_keywords(mock_llm, mock_retriever, make_chunk_doc)` (CHAIN-03 + streaming)
- Setup: chunk_doc = make_chunk_doc(). mock_retriever.ainvoke.return_value = [chunk_doc]. Set up mock_llm.astream to yield one token.
- Execute: NaiveChain with astream(query="q", hl_keywords=["provided_hl"], ll_keywords=["provided_ll"]).
- Assert: final_dict["keywords"] == {"high_level": ["provided_hl"], "low_level": ["provided_ll"]}. Keyword extraction was skipped, but keywords still appear in final dict.

**Pattern rules:**
- All test methods decorated with `@pytest.mark.asyncio` (astream is async generator)
- Use `async for chunk in chain.astream(...)` in test body (tests are async)
- `MagicMock(side_effect=_mock_stream)` is the pattern for mock async generators — side_effect receives an async generator function
- Note: mock_llm is `AsyncMock()` from conftest.py. Setting `mock_llm.astream = MagicMock(side_effect=...)` replaces the default auto-generated AsyncMock.astream.
  </action>
  <verify>
    <automated>python -m pytest tests/test_chain_stream.py -x -v 2>&1 | tail -25</automated>
  </verify>
  <done>
5 astream tests verifying: D-09 token-then-dict contract, D-10 sources-determined-before-streaming invariant, final dict contains all 4 required keys, empty LLM output edge case, and pre-provided keywords carried through to streaming output.
  </done>
  <acceptance_criteria>
- `tests/test_chain_stream.py` exists with 5 tests
- `pytest tests/test_chain_stream.py -x -v` exits 0
- Test verifies: `async for chunk in astream()` yields `str` then `dict` (D-09 contract)
- Test verifies: final dict has keys answer, sources, keywords, mode (CHAIN-02)
- Test verifies: reference list/sources present in final dict before any LLM token yields (D-10)
- Test verifies: empty LLM output yields answer == "" in final dict (not missing key)
- Test verifies: pre-provided keywords appear in streaming final dict
- All tests use only conftest.py fixtures — no real LLM or database
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mock → Chain | Tests inject controlled mocks — no real LLM, database, or network calls. All input is deterministic. |
| Chain output → Test assertions | Chain returns dict with user-visible fields (answer, sources, keywords, mode). Tests verify structure, not content validity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck. No new packages — test deps (pytest) already installed and verified. |
</threat_model>

<verification>
# Full test suite run for Phase 6 chain tests
echo "=== Chain test suite ==="
python -m pytest tests/test_chain_base.py tests/test_chain_keywords.py tests/test_chain_dispatch.py tests/test_chain_stream.py -x -v

echo ""
echo "=== Full regression suite ==="
python -m pytest tests/ -x --timeout=30 2>&1 | tail -10
</verification>

<success_criteria>
- [ ] All 4 test files created: test_chain_base.py, test_chain_keywords.py, test_chain_dispatch.py, test_chain_stream.py
- [ ] `pytest tests/test_chain_base.py tests/test_chain_keywords.py tests/test_chain_dispatch.py tests/test_chain_stream.py -x -v` — all tests pass
- [ ] Full regression: `pytest tests/ -x` — no existing tests broken
- [ ] CHAIN-01: pipeline integration tests pass (invoke/ainvoke dict structure, pipeline ordering)
- [ ] CHAIN-02: invoke/ainvoke/astream all return correct dict structure
- [ ] CHAIN-03: pre-provided keywords skip LLM extraction; partial keywords trigger extraction
- [ ] D-07: naive mode uses naive templates, KG modes use KG templates
- [ ] D-08: system_prompt parameter replaces entire prompt verbatim
- [ ] D-09: astream yields str tokens then final dict
- [ ] D-10: sources/keywords determined before any LLM token is yielded
- [ ] D-11/D-12: reference list dedup by file_path with sequential integer reference_ids
- [ ] All tests use mock fixtures only — zero real LLM calls, zero database connections
</success_criteria>

<output>
Create `.planning/phases/06-qa-chain/06-03-SUMMARY.md` when done
</output>
