---
phase: 04-query-strategies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lightrag_langchain/query/__init__.py
  - src/lightrag_langchain/query/results.py
  - tests/test_query_strategies.py
autonomous: true
requirements:
  - QUERY-01
  - QUERY-02
  - QUERY-03
  - QUERY-04
  - QUERY-05
  - QUERY-06

must_haves:
  truths:
    - "QueryResult is a frozen Pydantic model with entities, relations, chunks, and graph_triples fields"
    - "GraphTriple is a frozen Pydantic model with src_entity (GraphNode), relation (GraphEdge), and tgt_entity (GraphNode)"
    - "test_query_strategies.py exists and can be collected by pytest"
  artifacts:
    - path: "src/lightrag_langchain/query/results.py"
      provides: "QueryResult + GraphTriple Pydantic frozen models"
      contains: "class QueryResult"
    - path: "src/lightrag_langchain/query/__init__.py"
      provides: "Lazy __getattr__ placeholder for query strategy exports"
      contains: "__getattr__"
    - path: "tests/test_query_strategies.py"
      provides: "Test classes for QueryResult model, GraphTriple model, and placeholders for all 6 strategies"
      contains: "class TestQueryResult"
  key_links:
    - from: "query/results.py"
      to: "data/models.py"
      via: "import EntityRecord, RelationshipRecord, ChunkRecord, GraphNode, GraphEdge"
      pattern: "from lightrag_langchain\\.data\\.models import"
---

<objective>
Establish the query strategies package foundation: the QueryResult and GraphTriple Pydantic models per user decisions D-01 and D-02 (single union type with all fields), plus the test file scaffold with model shape tests.

Purpose: Give Plan 02 and Plan 03 a type contract to build strategy functions against. No strategy logic in this plan -- only data models and the test skeleton.

Output: `results.py` (~80 lines), `query/__init__.py` (~30 lines initial scaffold), `test_query_strategies.py` (~90 lines test scaffold)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-query-strategies/04-CONTEXT.md
@.planning/phases/04-query-strategies/04-RESEARCH.md
@src/lightrag_langchain/data/models.py
@src/lightrag_langchain/__init__.py
@src/lightrag_langchain/data/__init__.py

<interfaces>
## Key Types from Existing Code (DO NOT re-read -- use these directly)

From `data/models.py`:
- `EntityRecord(entity_name: str, content: str, source_id: str, file_path: str="", created_at: int|None=None)` -- frozen
- `RelationshipRecord(src_id: str, tgt_id: str, content: str|None=None, keywords: str|None=None, weight: float|None=None, created_at: int|None=None)` -- frozen
- `ChunkRecord(chunk_id: str, content: str, full_doc_id: str|None=None, chunk_order_index: int|None=None, file_path: str="")` -- frozen
- `GraphNode(entity_id: str, entity_type: str, description: str="", source_id: str="")` -- frozen
- `GraphEdge(source_id: str, target_id: str, description: str|None=None, keywords: str|None=None, weight: float|None=None)` -- frozen

From `data/__init__.py` (L:20-32): `__getattr__` lazy import pattern for PGVectorStore/PGGraphStore -- matches the pattern Plan 01 establishes for query/__init__.py.

From `__init__.py` (L:18-68): full `__getattr__` lazy export pattern with if-name chains and explicit `raise AttributeError` at end.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create query/results.py — QueryResult + GraphTriple models</name>
  <files>src/lightrag_langchain/query/results.py</files>
  <read_first>
    - src/lightrag_langchain/data/models.py (for EntityRecord, RelationshipRecord, ChunkRecord, GraphNode, GraphEdge imports and frozen pattern)
    - .planning/phases/04-query-strategies/04-CONTEXT.md (D-01, D-02 decisions about single union type)
    - .planning/phases/04-query-strategies/04-RESEARCH.md (lines 467-489 for exact QueryResult + GraphTriple model specs)
  </read_first>
  <action>
    Create the directory `src/lightrag_langchain/query/` if it does not exist.

    Create `src/lightrag_langchain/query/results.py` with two Pydantic models:

    1. **GraphTriple** (per D-04):
       - `model_config = ConfigDict(frozen=True)`
       - Fields: `src_entity: GraphNode`, `relation: GraphEdge`, `tgt_entity: GraphNode`
       - All three fields required (no default). Each contains the FULL properties of the source node, edge, and target node.
       - Use docstrings matching the existing style in `data/models.py` (triple-quoted after each field, same indentation pattern).

    2. **QueryResult** (per D-01, D-02):
       - `model_config = ConfigDict(frozen=True)`
       - Fields (all with empty list defaults, per D-02 union type design):
         - `entities: list[EntityRecord]` defaulting to empty list
         - `relations: list[RelationshipRecord]` defaulting to empty list
         - `chunks: list[ChunkRecord]` defaulting to empty list
         - `graph_triples: list[GraphTriple]` defaulting to empty list
       - Docstring explaining that each query mode fills only the relevant fields (naive fills chunks, local fills entities+graph_triples, global fills relations+graph_triples, hybrid fills entities+relations+graph_triples, mix fills all, bypass fills none).

    Imports: use `from __future__ import annotations` at top, then `from pydantic import BaseModel, ConfigDict`, then import the 5 data models from `lightrag_langchain.data.models` (EntityRecord, RelationshipRecord, ChunkRecord, GraphNode, GraphEdge).

    Follow the exact docstring/module-header style from `data/models.py` (module-level docstring with backticks around table names, blank lines between models, `# ---` separators).

    **Do NOT** import Pydantic's `Field` for defaults -- use bare `= []` and `= ""` patterns exactly as `data/models.py` does for all default values.
  </action>
  <verify>
    <automated>python3 -c "from lightrag_langchain.query.results import QueryResult, GraphTriple; q = QueryResult(); print(f'QueryResult: entities={len(q.entities)} chunks={len(q.chunks)} relations={len(q.relations)} triples={len(q.graph_triples)}')"</automated>
  </verify>
  <acceptance_criteria>
    - `QueryResult()` instantiates with all 4 fields as empty lists
    - `GraphTriple` has exactly `src_entity`, `relation`, `tgt_entity` required fields
    - Both models are `frozen=True` (attempting to set a field after construction raises ValidationError)
    - `python3 -c "from lightrag_langchain.query.results import QueryResult, GraphTriple"` succeeds without error
  </acceptance_criteria>
  <done>QueryResult + GraphTriple importable, frozen, with correct fields and defaults</done>
</task>

<task type="auto">
  <name>Task 2: Create query/__init__.py — initial lazy export scaffold</name>
  <files>src/lightrag_langchain/query/__init__.py</files>
  <read_first>
    - src/lightrag_langchain/__init__.py (full lazy __getattr__ pattern, lines 18-68)
    - src/lightrag_langchain/data/__init__.py (eager model exports + lazy store __getattr__, lines 1-32)
    - src/lightrag_langchain/data/models.py (for EagerRecord-style top-of-module imports with __all__)
  </read_first>
  <action>
    Create `src/lightrag_langchain/query/__init__.py` with a two-part structure:

    1. **Eager imports at module top** (matching `data/__init__.py` pattern):
       - `from lightrag_langchain.query.results import QueryResult, GraphTriple`
       - Define `__all__ = ["QueryResult", "GraphTriple"]` -- the models should always be importable. Strategy functions will be lazy (added in Plan 03).

    2. **`__getattr__` function** (matching `__init__.py` pattern):
       - Function signature: `def __getattr__(name: str):`
       - Docstring explaining lazy import pattern matches `data/__init__.py` and `lightrag_langchain/__init__.py`
       - For now: `raise AttributeError(f"module {__name__!r} has no attribute {name!r}")` -- strategy function entries will be added in Plan 03
       - Include placeholder comments like `# -- Strategy functions (added in Plan 03) --` before the `raise` statement

    Module docstring: describe the query package purpose: "LightRAG query strategy implementations (naive, local, global, hybrid, mix, bypass) with QueryResult intermediate representation."
  </action>
  <verify>
    <automated>python3 -c "from lightrag_langchain.query import QueryResult, GraphTriple; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from lightrag_langchain.query import QueryResult, GraphTriple` succeeds
    - `from lightrag_langchain.query import QueryResult` and `from lightrag_langchain.query import GraphTriple` both work
    - `lightrag_langchain.query.__all__` is `["QueryResult", "GraphTriple"]`
    - Accessing an undefined attribute raises AttributeError with the module name and attribute name
    - Importing `lightrag_langchain.query` does NOT trigger Settings instantiation (no .env required)
  </acceptance_criteria>
  <done>QueryResult and GraphTriple are importable via lightrag_langchain.query package</done>
</task>

<task type="auto">
  <name>Task 3: Create test_query_strategies.py — model shape tests + strategy placeholder test classes</name>
  <files>tests/test_query_strategies.py</files>
  <read_first>
    - tests/test_models.py (top of file: docstring format, import style, frozen=True test patterns)
    - tests/conftest.py (existing fixtures: mock_pool, mock_conn)
    - src/lightrag_langchain/data/models.py (EntityRecord field types for constructing test data)
    - .planning/phases/04-query-strategies/04-RESEARCH.md (lines 558-591 for requirements-to-test mapping)
  </read_first>
  <action>
    Create `tests/test_query_strategies.py` with these test classes:

    1. **TestQueryResultModel**: Tests for QueryResult Pydantic model
       - `test_empty_construction`: `QueryResult()` has all 4 fields as empty lists (len==0)
       - `test_frozen_immutable`: attempting `q.entities = []` after construction raises an error (use pytest.raises)
       - `test_fields_populated`: construct a QueryResult with 1 fake entity, 1 fake relation, and 1 fake chunk -- verify fields return correct counts

    2. **TestGraphTripleModel**: Tests for GraphTriple Pydantic model
       - `test_required_fields`: constructing GraphTriple without any field raises ValidationError
       - `test_full_construction`: construct with real GraphNode + GraphEdge + GraphNode and verify attribute access
       - `test_frozen_immutable`: attempting mutation after construction raises an error

    3. **Placeholder test classes** (one per strategy, each with 1 minimal test marked as `@pytest.mark.skip(reason="Implemented in Plan 02/03")`):
       - `TestNaiveStrategy`
       - `TestLocalStrategy`
       - `TestGlobalStrategy`
       - `TestHybridStrategy`
       - `TestMixStrategy`
       - `TestBypassStrategy`

    Follow `test_models.py` patterns:
    - Use `from __future__ import annotations` at top
    - Import pytest
    - Use `@pytest.mark.asyncio` for any async tests (future strategy tests)
    - Lazy imports of strategy functions inside test bodies (matching `_store_cls()` pattern from test_store.py)
    - Use descriptive test method names and docstrings

    Import test data construction helpers at module level:
    - `from lightrag_langchain.data.models import EntityRecord, RelationshipRecord, ChunkRecord, GraphNode, GraphEdge`

    For the placeholder test classes, each should contain ONE test method like:
    ```python
    class TestNaiveStrategy:
        @pytest.mark.skip(reason="Implemented in Plan 02")
        async def test_placeholder(self):
            pass
    ```

    Use module-level docstring: "Unit tests for QueryResult model, GraphTriple model, and all 6 query strategies."
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_query_strategies.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_query_strategies.py -x -q` exits 0 with all non-skipped tests passing
    - At least 6 test methods total: 3 in TestQueryResultModel, 3 in TestGraphTripleModel, 1 skipped in each of 6 placeholder classes
    - `QueryResult()` empty construction test passes
    - `GraphTriple` full construction test passes
    - Both frozen=True tests verify mutation raises error (use `pytest.raises`)
  </acceptance_criteria>
  <done>Test file has passing model shape tests and skipped strategy placeholders ready for Plan 02/03</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller -> query/results.py | Untrusted data passed to model constructors via `**kwargs` unpacking from database rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | QueryResult model construction | mitigate | Pydantic field validation on all fields -- incorrect types (e.g., str where GraphNode expected) raise ValidationError; frozen=True prevents post-construction mutation |
| T-04-02 | Information Disclosure | QueryResult contains entity/relation content | accept | No PII in knowledge graph data -- entity/relation content is public domain text; future phases may filter sensitive content but Phase 4 returns raw data per D-01 |
| T-04-SC | Tampering | No npm/pip/cargo packages installed in this plan | accept | Zero new dependencies; all models use stdlib + pydantic (already in pyproject.toml) |
</threat_model>

<verification>
  <automated>python3 -m pytest tests/test_query_strategies.py -x -q</automated>
  <automated>python3 -c "from lightrag_langchain.query import QueryResult, GraphTriple; from lightrag_langchain.query.results import QueryResult, GraphTriple; print('All imports OK')"</automated>
</verification>

<success_criteria>
- [ ] `src/lightrag_langchain/query/results.py` exists with QueryResult and GraphTriple frozen Pydantic models (D-01, D-02, D-04)
- [ ] `src/lightrag_langchain/query/__init__.py` exists with eager QueryResult + GraphTriple exports and lazy __getattr__ scaffold
- [ ] `tests/test_query_strategies.py` exists with passing model shape tests and 6 skipped placeholder test classes
- [ ] All imports work without .env or network (lazy pattern)
- [ ] All pytest tests pass (non-skipped)
</success_criteria>

<output>
Create `.planning/phases/04-query-strategies/04-01-SUMMARY.md` when done
</output>
