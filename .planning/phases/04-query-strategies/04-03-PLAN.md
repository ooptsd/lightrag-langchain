---
phase: 04-query-strategies
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/lightrag_langchain/query/strategies.py
  - src/lightrag_langchain/query/__init__.py
  - tests/test_query_strategies.py
autonomous: true
requirements:
  - QUERY-04
  - QUERY-05
  - QUERY-06

must_haves:
  truths:
    - "hybrid_strategy returns QueryResult with round-robin interleaved entities and relations from parallel local+global execution"
    - "mix_strategy returns QueryResult with hybrid results merged with chunk vector search results"
    - "bypass_strategy returns empty QueryResult with no database queries"
    - "Round-robin merge deduplicates entities by entity_name and relations by sorted(src_id, tgt_id) tuple per upstream LightRAG lines 3512-3566"
    - "query/__init__.py exports all 6 strategy functions + QueryResult + GraphTriple via lazy __getattr__"
  artifacts:
    - path: "src/lightrag_langchain/query/__init__.py"
      provides: "Lazy __getattr__ exports for all 6 strategies + eager exports for QueryResult, GraphTriple"
      contains: "__getattr__"
    - path: "src/lightrag_langchain/query/strategies.py"
      provides: "hybrid_strategy, mix_strategy, bypass_strategy + merge helpers appended after Plan 02's functions"
      contains: "async def hybrid_strategy"
  key_links:
    - from: "hybrid_strategy"
      to: "local_strategy + global_strategy"
      via: "asyncio.gather(local_strategy(...), global_strategy(...))"
      pattern: "asyncio\\.gather.*local_strategy.*global_strategy"
    - from: "mix_strategy"
      to: "hybrid_strategy + vector_store.search_chunks()"
      via: "asyncio.gather(hybrid_strategy(...), vector_store.search_chunks(...))"
      pattern: "search_chunks"
    - from: "query/__init__.py __getattr__"
      to: "query/strategies.py + query/results.py"
      via: "lazy import inside __getattr__ branches"
      pattern: "from lightrag_langchain\\.query\\.strategies import"
---

<objective>
Complete the query strategies module: implement the three composite strategies (hybrid, mix, bypass) that compose Plan 02's leaf strategies, add round-robin merge helpers matching upstream LightRAG behavior, update __init__.py with lazy exports for all 6 strategies, and replace placeholder skip marks with real strategy-level tests.

Purpose: Hybrid and mix require round-robin interleaving that matches upstream LightRAG lines 3512-3566. Bypass is a trivial empty-result function. The __init__.py lazy exports follow the established package pattern from `data/__init__.py`.

Output: strategies.py gets 3 new strategy functions + 3 merge helpers appended (~180 lines). query/__init__.py updated with __getattr__ branches for all strategies. tests updated with real (non-skipped) strategy-level integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-query-strategies/04-CONTEXT.md
@.planning/phases/04-query-strategies/04-RESEARCH.md
@src/lightrag_langchain/query/strategies.py
@src/lightrag_langchain/query/results.py
@src/lightrag_langchain/query/__init__.py
@src/lightrag_langchain/__init__.py
@src/lightrag_langchain/data/__init__.py

<interfaces>
## Functions created by Plan 02 -- import by executor for composition

From `lightrag_langchain.query.strategies`:
- `naive_strategy(query_embedding: list[float], *, vector_store: PGVectorStore, chunk_top_k: int | None = None) -> QueryResult`
- `local_strategy(query_embedding: list[float], *, vector_store: PGVectorStore, graph_store: PGGraphStore, top_k: int | None = None) -> QueryResult`
- `global_strategy(query_embedding: list[float], *, vector_store: PGVectorStore, graph_store: PGGraphStore, top_k: int | None = None) -> QueryResult`

## Round-robin merge algorithm (RESEARCH.md L:199-229, verified from upstream operate.py L:3512-3566)

Entity merge: iterate over `max(len(local), len(global))`, alternating local[i] then global[i], deduplicating by `entity_name` in a `seen` set.

Relation merge: same alternating pattern, deduplicating by `tuple(sorted([src_id, tgt_id]))` in a `seen` set.

## Current __init__.py state (from Plan 01)

Eager imports: QueryResult, GraphTriple from query.results
__all__: ["QueryResult", "GraphTriple"]
__getattr__: raises AttributeError with placeholder comment "# -- Strategy functions (added in Plan 03) --"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append hybrid_strategy + round-robin merge helpers to strategies.py</name>
  <files>src/lightrag_langchain/query/strategies.py</files>
  <read_first>
    - src/lightrag_langchain/query/strategies.py (current end of file -- append new functions after local_strategy)
    - .planning/phases/04-query-strategies/04-RESEARCH.md (L:199-229 round-robin algorithm, L:437-463 merge pattern, L:345-353 Pitfall 3 dedup)
  </read_first>
  <action>
    Append FIVE functions to `src/lightrag_langchain/query/strategies.py` below the existing local_strategy function.

    **Helper 1: `_round_robin_merge_entities`**
    - Exact signature: `def _round_robin_merge_entities(local_entities: list[EntityRecord], global_entities: list[EntityRecord]) -> list[EntityRecord]:`
    - Pure sync function
    - Implements the upstream pattern (RESEARCH.md L:201-222):
      - `merged: list[EntityRecord] = []`, `seen: set[str] = set()`, `max_len = max(len(local_entities), len(global_entities))`
      - `for i in range(max_len):`
        - If i < len(local_entities): get entity `local_entities[i]`, if `entity.entity_name` not in seen, append and add to seen
        - If i < len(global_entities): get entity `global_entities[i]`, if `entity.entity_name` not in seen, append and add to seen
      - Return merged list
    - Docstring references upstream `_perform_kg_search` lines 3512-3566

    **Helper 2: `_round_robin_merge_relations`**
    - Exact signature: `def _round_robin_merge_relations(local_relations: list[RelationshipRecord], global_relations: list[RelationshipRecord]) -> list[RelationshipRecord]:`
    - Same alternating pattern as entities, but dedup key is `tuple(sorted((r.src_id, r.tgt_id)))` as a tuple of two sorted strings (RESEARCH.md L:229)
    - Use a `set[tuple[str, str]]` for seen tracking

    **Helper 3: `_round_robin_merge_chunks`**
    - Exact signature: `def _round_robin_merge_chunks(vector_chunks: list[ChunkRecord], kg_chunks: list[ChunkRecord]) -> list[ChunkRecord]:`
    - Same alternating pattern, dedup key is `chunk.chunk_id`
    - "kg_chunks" refers to entity_content strings treated as pseudo-chunks (in mix mode, entity contents are treated as chunk-like text). For now, accept a flat list of ChunkRecord and merge by chunk_id.

    **Function 4: `hybrid_strategy` (QUERY-04)**
    - Exact signature: `async def hybrid_strategy(query_embedding: list[float], *, vector_store: PGVectorStore, graph_store: PGGraphStore, top_k: int | None = None) -> QueryResult:`
    - Lazy import settings inside function body, resolve top_k
    - Step 1: Use `asyncio.gather()` to run `local_strategy()` and `global_strategy()` in parallel with the same query_embedding, vector_store, graph_store, and _top_k
    - Step 2: Extract entities and relations from both results: `local_result.entities`, `local_result.relations` (empty from local per Plan 02), `global_result.entities` (empty from global per Plan 02), `global_result.relations`
    - Step 3: `merged_entities = _round_robin_merge_entities(local_result.entities, global_result.entities)` -- local provides entities, global provides (potentially empty) entities from entity lookup
    - Step 4: `merged_relations = _round_robin_merge_relations(local_result.relations, global_result.relations)` -- global provides most relations
    - Step 5: Merge graph_triples from both results with dedup key `(src.entity_id, tuple(sorted((edge.source_id, edge.target_id))), tgt.entity_id)`. Use a seen set, append from local first then global (interleave order matches upstream).
    - Step 6: Return `QueryResult(entities=merged_entities, relations=merged_relations, graph_triples=merged_triples)`
    - Docstring summarizing QUERY-04 behavior

    **Function 5: `bypass_strategy` (QUERY-06)**
    - Exact signature: `async def bypass_strategy() -> QueryResult:`
    - One line: `return QueryResult()` -- no parameters, no database queries, returns empty QueryResult per D-01 and RESEARCH.md L:497-500
    - Docstring summarizing QUERY-06 behavior
  </action>
  <verify>
    <automated>python3 -c "
import ast
tree = ast.parse(open('src/lightrag_langchain/query/strategies.py').read())
funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.AsyncFunctionDef)]
syncs = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and not isinstance(n, ast.AsyncFunctionDef)]
assert 'hybrid_strategy' in funcs, 'hybrid_strategy missing'
assert 'bypass_strategy' in funcs, 'bypass_strategy missing'
assert '_round_robin_merge_entities' in syncs, '_round_robin_merge_entities missing'
assert '_round_robin_merge_relations' in syncs, '_round_robin_merge_relations missing'
assert '_round_robin_merge_chunks' in syncs, '_round_robin_merge_chunks missing'
print(f'New functions added: {len(funcs)} async, {len(syncs)} sync total')
"</automated>
  </verify>
  <acceptance_criteria>
    - `hybrid_strategy` calls `asyncio.gather(local_strategy(...), global_strategy(...))` and merges results via round-robin helpers
    - `bypass_strategy()` takes no arguments and returns `QueryResult()` (all empty lists)
    - `_round_robin_merge_entities` follows the exact upstream alternating pattern with entity_name dedup
    - `_round_robin_merge_relations` deduplicates by `tuple(sorted((src_id, tgt_id)))`
    - `_round_robin_merge_chunks` deduplicates by `chunk_id`
    - All merge helpers are pure sync functions (no async, no I/O)
    - `from lightrag_langchain.query.strategies import hybrid_strategy, bypass_strategy` succeeds without .env
  </acceptance_criteria>
  <done>hybrid_strategy and bypass_strategy implemented with merge helpers matching upstream round-robin behavior</done>
</task>

<task type="auto">
  <name>Task 2: Append mix_strategy (QUERY-05) to strategies.py</name>
  <files>src/lightrag_langchain/query/strategies.py</files>
  <read_first>
    - src/lightrag_langchain/query/strategies.py (current end of file -- append after bypass_strategy)
    - .planning/phases/04-query-strategies/04-RESEARCH.md (L:44 description of mix, L:225-226 round-robin chunk merge pattern)
  </read_first>
  <action>
    Append `mix_strategy` to `src/lightrag_langchain/query/strategies.py` after the bypass_strategy function.

    **Function: `mix_strategy` (QUERY-05)**
    - Exact signature: `async def mix_strategy(query_embedding: list[float], *, vector_store: PGVectorStore, graph_store: PGGraphStore, top_k: int | None = None, chunk_top_k: int | None = None) -> QueryResult:`
    - Lazy import settings inside function body
    - Resolve top_k and chunk_top_k from settings if not provided
    - Step 1: Use `asyncio.gather()` to run `hybrid_strategy()` and `vector_store.search_chunks()` in parallel
    - Step 2: From hybrid_result: get entities, relations, and graph_triples
    - Step 3: From chunk search result: get chunks list
    - Step 4: Also collect entity content as pseudo-chunks: iterate hybrid_result.entities, for each entity construct a `ChunkRecord(chunk_id=entity.source_id, content=entity.content, full_doc_id=None, chunk_order_index=None, file_path=entity.file_path)`. These represent entity-text content mixed in with vector chunks.
    - Step 5: Merge vector chunks and entity chunks using `_round_robin_merge_chunks(vector_chunks, entity_chunks)`
    - Step 6: Return `QueryResult(entities=hybrid_result.entities, relations=hybrid_result.relations, chunks=merged_chunks, graph_triples=hybrid_result.graph_triples)`
    - Docstring summarizing QUERY-05 behavior
  </action>
  <verify>
    <automated>python3 -c "
import ast
tree = ast.parse(open('src/lightrag_langchain/query/strategies.py').read())
funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.AsyncFunctionDef)]
assert 'mix_strategy' in funcs, 'mix_strategy missing'
for name in ['naive_strategy','local_strategy','global_strategy','hybrid_strategy','mix_strategy','bypass_strategy']:
    assert name in funcs, f'{name} missing from strategies.py'
print('All 6 strategy functions present')
"</automated>
  </verify>
  <acceptance_criteria>
    - `mix_strategy` has signature `async def mix_strategy(query_embedding: list[float], *, vector_store: PGVectorStore, graph_store: PGGraphStore, top_k: int | None = None, chunk_top_k: int | None = None) -> QueryResult:`
    - mix_strategy calls `asyncio.gather(hybrid_strategy(...), vector_store.search_chunks(...))` for parallel execution
    - mix_strategy converts entity content to ChunkRecord objects for KG chunk merging
    - mix_strategy uses `_round_robin_merge_chunks()` to interleave vector chunks and entity chunks
    - mix_strategy returns QueryResult with all 4 fields populated (entities, relations, chunks, graph_triples)
    - `from lightrag_langchain.query.strategies import mix_strategy` succeeds
  </acceptance_criteria>
  <done>mix_strategy implemented merging hybrid results with vector chunk search (QUERY-05)</done>
</task>

<task type="auto">
  <name>Task 3: Update query/__init__.py with lazy exports for all 6 strategies, and update tests</name>
  <files>src/lightrag_langchain/query/__init__.py, tests/test_query_strategies.py</files>
  <read_first>
    - src/lightrag_langchain/query/__init__.py (current state from Plan 01 -- eager QueryResult/GraphTriple exports + empty __getattr__)
    - src/lightrag_langchain/__init__.py (L:18-68 for exact __getattr__ branch pattern with `if name ==` chains)
    - tests/test_query_strategies.py (current state from Plan 01 -- model tests + 6 skipped placeholder classes)
    - tests/conftest.py (mock_pool, mock_conn fixture signatures)
  </read_first>
  <action>
    **Part A: Update query/__init__.py**

    Replace the placeholder `__getattr__` function with one containing branches for all 6 strategy functions:

    - Keep the existing eager imports and __all__ definition from Plan 01 unchanged
    - Replace the placeholder comment "# -- Strategy functions (added in Plan 03) --" and `raise AttributeError(...)` with 6 branches:
      - `if name == "naive_strategy":` → `from lightrag_langchain.query.strategies import naive_strategy; return naive_strategy`
      - `if name == "local_strategy":` → `from lightrag_langchain.query.strategies import local_strategy; return local_strategy`
      - `if name == "global_strategy":` → `from lightrag_langchain.query.strategies import global_strategy; return global_strategy`
      - `if name == "hybrid_strategy":` → `from lightrag_langchain.query.strategies import hybrid_strategy; return hybrid_strategy`
      - `if name == "mix_strategy":` → `from lightrag_langchain.query.strategies import mix_strategy; return mix_strategy`
      - `if name == "bypass_strategy":` → `from lightrag_langchain.query.strategies import bypass_strategy; return bypass_strategy`
    - Keep `raise AttributeError(f"module {__name__!r} has no attribute {name!r}")` at the end for unknown names
    - Update `__all__` to include all 6 strategy function names

    Follow the exact pattern from `src/lightrag_langchain/__init__.py` lines 18-68: docstring inside function body for lazy import explanation, `if name == "NAME":` blocks with inline import and return, no blank lines between branches.

    **Part B: Update test_query_strategies.py**

    Replace the 6 placeholder skipped test classes with real test classes that test strategy functions with mock stores:

    1. **TestNaiveStrategy**: Two tests
       - `test_naive_returns_chunks`: mock vector_store.search_chunks returns [ChunkRecord(...)], verify QueryResult.chunks has 1 item and entities/relations/graph_triples are empty
       - `test_naive_weight_fallback`: with settings.query_params.kg_chunk_pick_method="WEIGHT", verify search_chunks is still called (falls back to VECTOR)

    2. **TestLocalStrategy**: One test
       - `test_local_returns_entities_and_triples`: mock search_entities returns 1 EntityRecord(entity_name="e1"), mock get_nodes_batch returns {"e1": GraphNode(...)}, mock get_node_edges returns [], verify QueryResult.entities populated and chunks/relations empty

    3. **TestGlobalStrategy**: One test
       - `test_global_returns_relations_and_triples`: mock search_relationships returns 1 RelationshipRecord(src_id="a", tgt_id="b"), mock get_nodes_batch returns proper GraphNode dict, mock get_edges_batch returns proper GraphEdge dict, verify QueryResult.relations and graph_triples populated

    4. **TestHybridStrategy**: One test
       - `test_hybrid_merges_local_and_global`: mock both local and global strategy internals (mock vector_store and graph_store appropriately), verify QueryResult has entities and relations round-robin merged

    5. **TestMixStrategy**: One test
       - `test_mix_includes_chunks`: mock hybrid internals + search_chunks, verify QueryResult.chunks is populated

    6. **TestBypassStrategy**: One test
       - `test_bypass_returns_empty`: call bypass_strategy(), verify all 4 fields are empty lists

    Create test helper functions for constructing mock data:
    - `_make_entity(name="test-entity", content="test content") -> EntityRecord`
    - `_make_relation(src="a", tgt="b") -> RelationshipRecord`
    - `_make_chunk(chunk_id="c1", content="chunk text") -> ChunkRecord`
    - `_make_graph_node(entity_id="e1") -> GraphNode`
    - `_make_graph_edge(src="a", tgt="b") -> GraphEdge`

    All strategy tests use `@pytest.mark.asyncio` decorator. Use `from unittest.mock import AsyncMock, patch` for mocking store methods. Us

    **IMPORTANT -- lazy import pattern for tests:** Since strategies.py lazily imports `settings` inside function bodies, tests can monkeypatch `lightrag_langchain.config.settings` before calling strategy functions without needing a real .env file. Use `conftest.py`'s `mock_pool` and `mock_conn` fixtures where PGVectorStore/PGGraphStore needs pool injection.

    Use `patch.object` to mock `vector_store.search_chunks = AsyncMock(...)`, `graph_store.get_nodes_batch = AsyncMock(...)`, etc. Do NOT instantiate real PGVectorStore/PGGraphStore objects in unit tests.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_query_strategies.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `from lightrag_langchain.query import naive_strategy, local_strategy, global_strategy, hybrid_strategy, mix_strategy, bypass_strategy` succeeds via lazy __getattr__
    - `from lightrag_langchain.query import QueryResult, GraphTriple` still works (eager exports preserved)
    - `lightrag_langchain.query.__all__` includes all 6 strategy function names + QueryResult + GraphTriple
    - All placeholder skip marks removed from test file
    - At least 8 test methods: 3 model tests + 2 naive tests + 1 each for local/global/hybrid/mix/bypass = 8+
    - `python3 -m pytest tests/test_query_strategies.py -x -q` exits 0 with all tests passing
    - Accessing undefined attribute like `lightrag_langchain.query.nonexistent` raises AttributeError
  </acceptance_criteria>
  <done>__init__.py exports all 6 strategies via lazy __getattr__, tests cover all strategies with mock stores</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hybrid_strategy -> asyncio.gather | Two concurrent I/O paths (local + global) -- one failure should not crash the other |
| Caller -> __init__.py __getattr__ | Untrusted attribute name string -- handled by explicit if-name branches + final AttributeError |
| mix_strategy -> hybrid_strategy + search_chunks | Two concurrent I/O paths -- hybrid composes local+global internally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Denial of Service | asyncio.gather in hybrid/mix strategies | mitigate | asyncio.gather default behavior raises on first exception -- consider using `return_exceptions=False` so partial results are discarded cleanly rather than hanging; both local and global strategies return empty QueryResult on errors (don't raise) |
| T-04-05 | Information Disclosure | Error messages from strategy functions | mitigate | Strategies return empty QueryResult() on error paths; no raise propagation; __getattr__ raises AttributeError with only module and attribute names (no internal state) |
| T-04-06 | Tampering | Attribute injection via __getattr__ | mitigate | Explicit if-name whitelist with final raise AttributeError -- only known strategy names resolve; no dynamic attribute resolution |
| T-04-SC | Tampering | No new packages | accept | Zero new dependencies |
</threat_model>

<verification>
  <automated>python3 -m pytest tests/test_query_strategies.py -x -q</automated>
  <automated>python3 -c "
from lightrag_langchain.query import (naive_strategy, local_strategy, global_strategy, hybrid_strategy, mix_strategy, bypass_strategy, QueryResult, GraphTriple)
import inspect
for name, obj in [('naive_strategy', naive_strategy), ('local_strategy', local_strategy), ('global_strategy', global_strategy), ('hybrid_strategy', hybrid_strategy), ('mix_strategy', mix_strategy), ('bypass_strategy', bypass_strategy)]:
    assert inspect.iscoroutinefunction(obj), f'{name} is not async'
print('All 6 strategies are async functions, exported via lazy __getattr__')
"</automated>
</verification>

<success_criteria>
- [ ] `hybrid_strategy` implemented with asyncio.gather + round-robin entity/relation/graph_triple merge under dedup (QUERY-04)
- [ ] `mix_strategy` implemented with hybrid + chunk search + entity-as-chunk merging (QUERY-05)
- [ ] `bypass_strategy` implemented as no-op returning empty QueryResult (QUERY-06)
- [ ] `_round_robin_merge_entities`, `_round_robin_merge_relations`, `_round_robin_merge_chunks` helpers match upstream LightRAG dedup logic
- [ ] `query/__init__.py` exports all 6 strategies via lazy __getattr__ (no eager import of strategies.py)
- [ ] `query/__init__.py` still eagerly exports QueryResult and GraphTriple from Plan 01
- [ ] All 8+ tests pass in test_query_strategies.py
- [ ] All 6 strategy function signatures verified as async with correct parameter shapes
</success_criteria>

<output>
Create `.planning/phases/04-query-strategies/04-03-SUMMARY.md` when done
</output>
