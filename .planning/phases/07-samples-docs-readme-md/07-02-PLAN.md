---
phase: 07-samples-docs-readme-md
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - docs/api-reference/index.md
  - docs/api-reference/chains.md
  - docs/api-reference/retrievers.md
  - docs/api-reference/factories.md
  - docs/api-reference/reranker.md
  - docs/api-reference/keywords.md
  - docs/api-reference/token-budget.md
  - docs/api-reference/config.md
autonomous: true
requirements: [D-03, D-04]

must_haves:
  truths:
    - "Every public API class/function has auto-generated documentation visible in the rendered MkDocs site, displaying function signatures, parameter descriptions, and type annotations"
    - "All API reference pages render correctly when browsed in the MkDocs site — no 'Could not find' errors or broken symbol references"
    - "Code examples from source docstrings appear inline in the rendered API documentation, showing runnable import-and-call patterns (not pseudocode)"
    - "The API reference overview page (api-reference/index.md) links to all 7 sub-pages, and each sub-page is accessible from the navigation sidebar"
    - "All documented symbols use their actual source module paths, ensuring the MkDocs site shows correct canonical import paths for every class/function"
  artifacts:
    - path: "docs/api-reference/index.md"
      provides: "Overview of API reference sections with links to sub-pages"
    - path: "docs/api-reference/chains.md"
      provides: "LightRAGBaseChain + 6 chain subclasses (7 ::: directives)"
      contains: "::: lightrag_langchain.chain"
    - path: "docs/api-reference/retrievers.md"
      provides: "LightRAGBaseRetriever + 6 retriever subclasses (7 ::: directives)"
      contains: "::: lightrag_langchain.retriever"
    - path: "docs/api-reference/factories.md"
      provides: "create_llm(), create_embedding(), create_reranker() (3 ::: directives)"
      contains: "::: lightrag_langchain.llm.create_llm"
    - path: "docs/api-reference/reranker.md"
      provides: "LightRAGReranker + Reranker Protocol (2 ::: directives)"
      contains: "::: lightrag_langchain.reranker"
    - path: "docs/api-reference/keywords.md"
      provides: "extract_keywords() + KeywordsSchema (2 ::: directives)"
      contains: "::: lightrag_langchain.keywords"
    - path: "docs/api-reference/token-budget.md"
      provides: "truncate_entities_by_tokens(), truncate_relations_by_tokens(), compute_chunk_token_budget() (3 ::: directives)"
      contains: "::: lightrag_langchain.token_budget"
    - path: "docs/api-reference/config.md"
      provides: "Settings + 5 config sub-models + SettingsError + settings singleton (7+ ::: directives)"
      contains: "::: lightrag_langchain.config"
  key_links:
    - from: "docs/api-reference/chains.md"
      to: "src/lightrag_langchain/chain/base.py + chains.py"
      via: "::: directive statically parsed by Griffe"
      pattern: "^::: lightrag_langchain\\.chain\\."
    - from: "docs/api-reference/retrievers.md"
      to: "src/lightrag_langchain/retriever/base.py + retrievers.py"
      via: "::: directive statically parsed by Griffe"
      pattern: "^::: lightrag_langchain\\.retriever\\."
---

<objective>
Create all 8 API reference pages under docs/api-reference/ that auto-generate documentation
from Python docstrings using mkdocstrings ::: directives. Cover every public class, function,
and model exported from the lightrag_langchain package.

Purpose: Per D-03 (all public API documented) and D-04 (each entry includes a minimal usage example
inline from source docstrings). Before writing pages, audit that source docstrings contain
usage examples — if any are missing, add them to the source files first.

Output: 8 Markdown files in docs/api-reference/ with Chinese section headers, concise descriptions,
and explicit ::: directives referencing the actual source module paths (not lazy re-export paths,
per RESEARCH Pitfall 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-samples-docs-readme-md/07-RESEARCH.md (Pattern 2: mkdocstrings Auto-Generation Directive, lines 271-309; Pitfall 1: Lazy __getattr__ Exports Not Discovered by Griffe, lines 456-466)
@src/lightrag_langchain/__init__.py (verify which symbols are publicly exported)
@src/lightrag_langchain/chain/__init__.py (chain exports)
@src/lightrag_langchain/chain/base.py (LightRAGBaseChain class)
@src/lightrag_langchain/chain/chains.py (6 chain subclasses)
@src/lightrag_langchain/retriever/__init__.py (retriever exports)
@src/lightrag_langchain/retriever/base.py (LightRAGBaseRetriever class)
@src/lightrag_langchain/retriever/retrievers.py (6 retriever subclasses)
@src/lightrag_langchain/llm.py (create_llm, create_embedding)
@src/lightrag_langchain/reranker.py (create_reranker, LightRAGReranker, Reranker Protocol)
@src/lightrag_langchain/keywords.py (extract_keywords, KeywordsSchema)
@src/lightrag_langchain/token_budget.py (3 token budget functions)
@src/lightrag_langchain/config.py (Settings, 5 sub-models, SettingsError, settings singleton)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit source docstrings for D-04 usage examples, then create API reference pages — Chains, Retrievers, Factories, Config</name>
  <files>
    docs/api-reference/chains.md,
    docs/api-reference/retrievers.md,
    docs/api-reference/factories.md,
    docs/api-reference/config.md
  </files>
  <read_first>
    - src/lightrag_langchain/chain/base.py (LightRAGBaseChain — its docstring, __init__ params, public methods: invoke, ainvoke, astream)
    - src/lightrag_langchain/chain/chains.py (NaiveChain, LocalChain, GlobalChain, HybridChain, MixChain, BypassChain — class docstrings and mode attribute)
    - src/lightrag_langchain/retriever/base.py (LightRAGBaseRetriever — constructor params: vector_store, embedding_config, graph_store, top_k, chunk_top_k)
    - src/lightrag_langchain/retriever/retrievers.py (6 retriever subclasses — class docstrings)
    - src/lightrag_langchain/llm.py (create_llm, create_embedding — function signatures and docstrings)
    - src/lightrag_langchain/reranker.py (create_reranker — function signature)
    - src/lightrag_langchain/config.py (Settings, PgConfig, LlmConfig, EmbeddingConfig, RerankerConfig, QueryParamsConfig, SettingsError, settings singleton)
    - .planning/phases/07-samples-docs-readme-md/07-RESEARCH.md (Pattern 2 for ::: syntax; lines 271-309)
  </read_first>
  <action>
    **STEP 0 — D-04 Docstring Audit (pre-execution verification):**

    Before writing any API reference pages, verify that each of the following symbols has a
    usage example section in its docstring (Google-style "Example:" or code block).
    For each symbol, check: does the docstring contain a code block showing how to import
    and use the class/function?

    Symbols to audit:
    - LightRAGBaseChain, NaiveChain, LocalChain, GlobalChain, HybridChain, MixChain, BypassChain (7 chain classes)
    - LightRAGBaseRetriever, NaiveRetriever, LocalRetriever, GlobalRetriever, HybridRetriever, MixRetriever, BypassRetriever (7 retriever classes)
    - create_llm, create_embedding, create_reranker (3 factory functions)
    - LightRAGReranker, Reranker (2 reranker symbols)
    - extract_keywords, KeywordsSchema (2 keyword symbols)
    - truncate_entities_by_tokens, truncate_relations_by_tokens, compute_chunk_token_budget (3 token budget functions)
    - Settings, PgConfig, LlmConfig, EmbeddingConfig, RerankerConfig, QueryParamsConfig, SettingsError, settings (8 config symbols)

    Audit method: For each symbol, grep its source file for "Example:" or "```python" patterns
    within the docstring. Count how many symbols have docstring examples vs. how many don't.

    If any symbol lacks a usage example in its docstring:
    - Add a minimal "Example:" section to that symbol's docstring showing import + call pattern
    - Use Google-style docstring format (consistent with existing docstrings)
    - Example format:
      ```
      Example:
          ```python
          from lightrag_langchain import NaiveChain, NaiveRetriever
          chain = NaiveChain(retriever=retriever, llm=llm)
          result = await chain.ainvoke("your question")
          ```
      ```
    - Commit docstring additions with message: "docs(src): add usage examples to docstrings for D-04 compliance"

    Record audit results in SUMMARY.md: total symbols checked, symbols that already had examples,
    symbols that needed examples added.

    **STEP 1 — Create 4 API reference pages:**

    Each page has:
    - A Chinese-language section header (h1 for page title) and brief description paragraph
    - ::: directives for each symbol using the ACTUAL SOURCE MODULE PATH (Pitfall 1: never use lazy re-export paths)

    **1. docs/api-reference/chains.md** — Title: "Chain 接口 (Chains)"
    Description: Brief Chinese paragraph explaining that chains encapsulate the full QA pipeline (keyword extraction → retrieval → token budget → context assembly → LLM generation).
    ::: directives:
    - ::: lightrag_langchain.chain.base.LightRAGBaseChain (with options: members: [invoke, ainvoke, astream])
    - ::: lightrag_langchain.chain.chains.NaiveChain
    - ::: lightrag_langchain.chain.chains.LocalChain
    - ::: lightrag_langchain.chain.chains.GlobalChain
    - ::: lightrag_langchain.chain.chains.HybridChain
    - ::: lightrag_langchain.chain.chains.MixChain
    - ::: lightrag_langchain.chain.chains.BypassChain

    **2. docs/api-reference/retrievers.md** — Title: "Retriever 接口 (Retrievers)"
    Description: Brief Chinese paragraph explaining retrievers fetch documents from the knowledge graph and return LangChain Document objects.
    ::: directives:
    - ::: lightrag_langchain.retriever.base.LightRAGBaseRetriever
    - ::: lightrag_langchain.retriever.retrievers.NaiveRetriever
    - ::: lightrag_langchain.retriever.retrievers.LocalRetriever
    - ::: lightrag_langchain.retriever.retrievers.GlobalRetriever
    - ::: lightrag_langchain.retriever.retrievers.HybridRetriever
    - ::: lightrag_langchain.retriever.retrievers.MixRetriever
    - ::: lightrag_langchain.retriever.retrievers.BypassRetriever

    **3. docs/api-reference/factories.md** — Title: "工厂函数 (Factory Functions)"
    Description: Brief Chinese paragraph explaining factories create LLM, embedding, and reranker instances from config.
    ::: directives:
    - ::: lightrag_langchain.llm.create_llm
    - ::: lightrag_langchain.llm.create_embedding
    - ::: lightrag_langchain.reranker.create_reranker

    **4. docs/api-reference/config.md** — Title: "配置 (Configuration)"
    Description: Brief Chinese paragraph explaining the typed configuration system — Settings singleton, 5 sub-models, env-based loading.
    ::: directives:
    - ::: lightrag_langchain.config.Settings
    - ::: lightrag_langchain.config.PgConfig
    - ::: lightrag_langchain.config.LlmConfig
    - ::: lightrag_langchain.config.EmbeddingConfig
    - ::: lightrag_langchain.config.RerankerConfig
    - ::: lightrag_langchain.config.QueryParamsConfig
    - ::: lightrag_langchain.config.SettingsError
    - ::: lightrag_langchain.config.settings

    Writing rule: Do NOT include fenced code blocks of example usage inside the Markdown pages.
    The docstrings in source code already contain usage examples (verified in Step 0 audit, with
    any gaps remediated). mkdocstrings will render these automatically.

    After writing all 4 files, run "uv run mkdocs build --strict" to verify all ::: directives resolve.
  </action>
  <verify>
    <automated>uv run mkdocs build --strict 2>&1 | grep -E "ERROR|WARNING|Building" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - D-04 docstring audit completed and recorded (symbols checked, examples found, gaps filled)
    - docs/api-reference/chains.md exists with 7 ::: directives (1 base + 6 subclasses)
    - docs/api-reference/retrievers.md exists with 7 ::: directives (1 base + 6 subclasses)
    - docs/api-reference/factories.md exists with 3 ::: directives (create_llm, create_embedding, create_reranker)
    - docs/api-reference/config.md exists with 8 ::: directives (Settings, 5 sub-models, SettingsError, settings)
    - All ::: directives use the source module path (e.g. "lightrag_langchain.chain.chains.NaiveChain"), never the lazy re-export path (e.g. "lightrag_langchain.NaiveChain")
    - "uv run mkdocs build --strict" exits 0 with no "Could not find" errors from mkdocstrings
    - Each page has a Chinese h1 title and descriptive paragraph
  </acceptance_criteria>
  <done>4 API reference pages created; D-04 docstring audit complete; mkdocs build passes with all ::: directives resolving correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Create API reference pages — Reranker, Keywords, Token Budget, Overview Index</name>
  <files>
    docs/api-reference/reranker.md,
    docs/api-reference/keywords.md,
    docs/api-reference/token-budget.md,
    docs/api-reference/index.md
  </files>
  <read_first>
    - src/lightrag_langchain/reranker.py (LightRAGReranker class, Reranker Protocol — docstrings and constructor params; also create_reranker if not already read)
    - src/lightrag_langchain/keywords.py (extract_keywords function, KeywordsSchema model — docstrings and fields)
    - src/lightrag_langchain/token_budget.py (truncate_entities_by_tokens, truncate_relations_by_tokens, compute_chunk_token_budget — function signatures and docstrings)
    - .planning/phases/07-samples-docs-readme-md/07-RESEARCH.md (Pattern 2 for ::: syntax)
  </read_first>
  <action>
    Create 4 API reference pages. Same rules as Task 1: Chinese headers + descriptions + ::: directives using actual source module paths.

    **1. docs/api-reference/reranker.md** — Title: "Reranker 接口 (Reranker)"
    Description: Brief Chinese paragraph explaining the multi-backend reranker system (aliyun, cohere, jina) with LightRAGReranker as a LangChain BaseDocumentCompressor.
    ::: directives:
    - ::: lightrag_langchain.reranker.LightRAGReranker
    - ::: lightrag_langchain.reranker.Reranker

    **2. docs/api-reference/keywords.md** — Title: "关键词提取 (Keyword Extraction)"
    Description: Brief Chinese paragraph explaining LLM-based keyword extraction (high-level and low-level keywords) used internally by chains.
    ::: directives:
    - ::: lightrag_langchain.keywords.extract_keywords
    - ::: lightrag_langchain.keywords.KeywordsSchema

    **3. docs/api-reference/token-budget.md** — Title: "Token 预算控制 (Token Budget)"
    Description: Brief Chinese paragraph explaining the token budget system — entity truncation, relation truncation, and chunk budget calculation.
    ::: directives:
    - ::: lightrag_langchain.token_budget.truncate_entities_by_tokens
    - ::: lightrag_langchain.token_budget.truncate_relations_by_tokens
    - ::: lightrag_langchain.token_budget.compute_chunk_token_budget

    **4. docs/api-reference/index.md** — Title: "API 参考 (API Reference)"
    Description: Overview page with a Chinese paragraph explaining the API structure, followed by a bulleted list of links to all 7 sub-pages:
    - [Chains](api-reference/chains.md)
    - [Retrievers](api-reference/retrievers.md)
    - [Factories](api-reference/factories.md)
    - [Reranker](api-reference/reranker.md)
    - [Keywords](api-reference/keywords.md)
    - [Token Budget](api-reference/token-budget.md)
    - [Configuration](api-reference/config.md)

    After writing all 4 files, run "uv run mkdocs build --strict" to verify all ::: directives resolve.
  </action>
  <verify>
    <automated>uv run mkdocs build --strict 2>&1 | grep -E "ERROR|WARNING|Building" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - docs/api-reference/reranker.md exists with 2 ::: directives (LightRAGReranker, Reranker)
    - docs/api-reference/keywords.md exists with 2 ::: directives (extract_keywords, KeywordsSchema)
    - docs/api-reference/token-budget.md exists with 3 ::: directives (truncate_entities_by_tokens, truncate_relations_by_tokens, compute_chunk_token_budget)
    - docs/api-reference/index.md exists with links to all 7 sub-pages
    - "uv run mkdocs build --strict" exits 0 with no "Could not find" errors
    - All ::: directives use source module paths, not lazy re-export paths (Pitfall 1)
    - Total ::: directive count across all 8 api-reference pages is at least 28
  </acceptance_criteria>
  <done>4 API reference pages created; overview index links all sub-pages; mkdocs build passes for entire api-reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time → Source code | mkdocstrings reads source files via Griffe static analysis; no runtime code execution |
| Build-time → Output | Generated HTML from docstrings; if docstrings contain sensitive data, it appears in output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | Source code docstrings | accept | Docstrings are already public in the source repository. mkdocstrings only republishes what is already visible in source code. No new information exposure. |
| T-07-04 | Denial of Service | mkdocstrings static analysis | accept | Griffe operates at build time (no runtime attack surface). Build timeout is inherent CI constraint. |
</threat_model>

<verification>
Run "uv run mkdocs build --strict" — must exit 0.
Count ::: directives: grep -c "^:::" docs/api-reference/*.md — must return at least 28.
Verify every ::: directive path matches a real Python module path in src/.
Check that no ::: directive uses the lazy re-export path (e.g. "lightrag_langchain.NaiveChain" is FORBIDDEN; "lightrag_langchain.chain.chains.NaiveChain" is CORRECT).
</verification>

<success_criteria>
- [ ] 8 API reference Markdown files created in docs/api-reference/
- [ ] Every public API symbol has a ::: directive (at least 28 total)
- [ ] All ::: directives use source module paths (Pitfall 1 avoided)
- [ ] uv run mkdocs build --strict exits 0
- [ ] Each page has a Chinese title and description paragraph
- [ ] docs/api-reference/index.md links to all 7 sub-pages
- [ ] D-04 docstring audit completed: all documented symbols have usage examples in their docstrings
</success_criteria>

<output>
Create `.planning/phases/07-samples-docs-readme-md/07-02-SUMMARY.md` when done
</output>
