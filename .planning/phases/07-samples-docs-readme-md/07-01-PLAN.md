---
phase: 07-samples-docs-readme-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - mkdocs.yml
  - .github/workflows/deploy-mkdocs.yml
  - docs/.gitkeep
  - docs/api-reference/.gitkeep
autonomous: true
requirements: [D-01, D-02, D-06]

must_haves:
  truths:
    - "uv run mkdocs build --strict exits with code 0"
    - "mkdocs.yml exists at project root and is valid YAML with Material theme configured"
    - ".github/workflows/deploy-mkdocs.yml exists and has correct permissions (contents: write)"
    - "docs/ directory tree exists with docs/api-reference/ subdirectory"
  artifacts:
    - path: "pyproject.toml"
      provides: "Dev dependency declarations for mkdocs-material, mkdocstrings, mkdocstrings-python"
      contains: "mkdocs-material"
    - path: "mkdocs.yml"
      provides: "MkDocs configuration with Material theme + mkdocstrings Python handler"
      contains: "theme:"
    - path: ".github/workflows/deploy-mkdocs.yml"
      provides: "GitHub Actions CI workflow deploying docs to GitHub Pages on push to main"
      contains: "mkdocs gh-deploy"
    - path: "docs/.gitkeep"
      provides: "docs/ directory placeholder"
    - path: "docs/api-reference/.gitkeep"
      provides: "docs/api-reference/ directory placeholder"
  key_links:
    - from: "mkdocs.yml"
      to: "docs/"
      via: "nav entries pointing to docs/*.md files"
      pattern: "nav:"
    - from: ".github/workflows/deploy-mkdocs.yml"
      to: "mkdocs gh-deploy"
      via: "CI step running mkdocs gh-deploy --force"
      pattern: "gh-deploy"

---

<objective>
Set up the documentation infrastructure: install MkDocs + Material + mkdocstrings dev dependencies,
create the mkdocs.yml configuration file, scaffold the docs/ directory tree, and create the
GitHub Actions CI workflow that auto-deploys the docs site to GitHub Pages on every push to main.

Purpose: Per D-01 (MkDocs + Material), D-02 (docs/ directory structure), and D-06 (GitHub Pages via CI).
Without this infrastructure, API reference pages and user-facing docs cannot be built or deployed.

Output: Updated pyproject.toml with 3 dev dependencies, mkdocs.yml valid and buildable,
.github/workflows/deploy-mkdocs.yml ready for CI, docs/ directory tree scaffolded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/07-samples-docs-readme-md/07-CONTEXT.md
@.planning/phases/07-samples-docs-readme-md/07-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MkDocs dev dependencies to pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml — current dev dependency groups (both [project.optional-dependencies] and [dependency-groups])
  </read_first>
  <action>
    Add mkdocs-material (9.7.6), mkdocstrings (1.0.4), and mkdocstrings-python (2.0.3) as dev dependencies.

    Run: uv add --group dev mkdocs-material mkdocstrings mkdocstrings-python

    Confirm after execution that pyproject.toml [dependency-groups].dev contains all three with version pins.
    Do NOT manually edit pyproject.toml — use "uv add" so uv.lock is kept in sync.

    Package legitimacy: All three packages pass slopcheck [OK] per RESEARCH.md Package Legitimacy Audit.
    No install task is needed — they are already verified in the audit table.
  </action>
  <verify>
    <automated>grep -c "mkdocs-material" pyproject.toml && grep -c "mkdocstrings" pyproject.toml && grep -c "mkdocstrings-python" pyproject.toml || echo "HUMAN CHECK: verify grep output"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml [dependency-groups].dev contains "mkdocs-material>=", "mkdocstrings>=", and "mkdocstrings-python>="
    - uv.lock is updated (no lock conflict)
    - "uv run mkdocs --version" prints the MkDocs version (proves packages installed correctly)
  </acceptance_criteria>
  <done>pyproject.toml has 3 new dev deps; uv sync installs them; mkdocs --version works.</done>
</task>

<task type="auto">
  <name>Task 2: Create mkdocs.yml with Material theme + mkdocstrings configuration</name>
  <files>mkdocs.yml</files>
  <read_first>
    - .planning/phases/07-samples-docs-readme-md/07-RESEARCH.md (Pattern 1: mkdocs.yml Configuration, lines 200-269)
    - pyproject.toml (verify src/ layout: packages = ["src/lightrag_langchain"])
    - src/lightrag_langchain/__init__.py (verify lazy __getattr__ pattern — no __all__)
    - src/lightrag_langchain/chain/__init__.py (verify chain exports)
    - src/lightrag_langchain/retriever/__init__.py (verify retriever exports)
  </read_first>
  <action>
    Create mkdocs.yml at project root with the following exact configuration (derived from RESEARCH.md Pattern 1):

    site_name: lightrag-langchain
    site_description: LangChain-based read-only query layer for LightRAG knowledge graphs
    repo_url: determine from "git remote get-url origin" — if unavailable, use placeholder "https://github.com/USER/lightrag-langchain"
    repo_name: lightrag-langchain

    theme:
      name: material
      language: zh
      features: [navigation.instant, navigation.tracking, navigation.sections, navigation.expand, search.suggest, search.highlight, content.code.copy, content.code.annotate]

    plugins:
      - search
      - mkdocstrings:
          default_handler: python
          handlers:
            python:
              paths: [src]           # CRITICAL: src/ layout per RESEARCH Pitfall 2
              options:
                show_source: true
                show_root_heading: true
                heading_level: 2
                docstring_style: google    # CRITICAL: Google-style per RESEARCH Pitfall 5
                members_order: source
                group_by_category: true
                show_signature_annotations: true
                separate_signature: true
                filters: ["!^_"]            # Exclude private/single-underscore members

    markdown_extensions:
      - admonition
      - pymdownx.highlight:
          anchor_linenums: true
      - pymdownx.superfences
      - pymdownx.inlinehilite
      - pymdownx.snippets
      - pymdownx.details
      - pymdownx.tabbed:
          alternate_style: true
      - toc:
          permalink: true

    nav:
      - Home: index.md
      - Quick Start: quick-start.md
      - API Reference:
        - Overview: api-reference/index.md
        - Chains: api-reference/chains.md
        - Retrievers: api-reference/retrievers.md
        - Factories: api-reference/factories.md
        - Reranker: api-reference/reranker.md
        - Keywords: api-reference/keywords.md
        - Token Budget: api-reference/token-budget.md
        - Configuration: api-reference/config.md
      - Examples: examples.md

    docs_dir: docs
    site_dir: site

    Do NOT include site_url yet — the GitHub Pages URL will only be correct after the first deploy.
    After first deploy, the URL can be set to https://<user>.github.io/lightrag-langchain/.

    Validate: run "uv run mkdocs build --strict" after creation — must exit 0.
  </action>
  <verify>
    <automated>uv run mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - mkdocs.yml exists at project root (not in a subdirectory)
    - "uv run mkdocs build --strict" exits with code 0 (site/ directory created)
    - Config contains all required sections: site_name, theme (material, language: zh), plugins (mkdocstrings with paths: [src]), markdown_extensions, nav (8 entries)
    - docstring_style is set to "google" (not sphinx/numpy)
    - paths is ["src"] under handlers.python, NOT under plugins.mkdocstrings (Pitfall 2)
  </acceptance_criteria>
  <done>mkdocs.yml exists, mkdocs build --strict exits 0 with generated site/ directory.</done>
</task>

<task type="auto">
  <name>Task 3: Create CI deployment workflow + scaffold docs/ directory tree</name>
  <files>.github/workflows/deploy-mkdocs.yml, docs/.gitkeep, docs/api-reference/.gitkeep</files>
  <read_first>
    - .planning/phases/07-samples-docs-readme-md/07-RESEARCH.md (Pattern 3: GitHub Actions Deployment Workflow, lines 319-355)
    - pyproject.toml (confirm python-version requirement: >=3.12)
  </read_first>
  <action>
    Create two items:

    1. .github/workflows/deploy-mkdocs.yml — exact content from RESEARCH.md Pattern 3:
       - name: Deploy MkDocs
       - trigger: push to main + workflow_dispatch
       - permissions: contents: write
       - jobs.deploy: runs-on ubuntu-latest, steps:
         * actions/checkout@v4
         * Configure Git Credentials (github-actions[bot] user/email)
         * actions/setup-python@v5 with python-version "3.12"
         * Cache MkDocs dependencies (actions/cache@v4, key: mkdocs-material-${{ github.run_id }}, path: ~/.cache/pip)
         * pip install mkdocs-material mkdocstrings mkdocstrings-python
         * mkdocs gh-deploy --force
       - Do NOT include any secrets or API keys. The workflow only uses GITHUB_TOKEN (auto-provisioned by GitHub Actions).
       - Do NOT modify, extend, or "improve" the RESEARCH.md template — use it exactly as shown in Pattern 3.

    2. Scaffold docs/ directory tree:
       - Run: mkdir -p docs/api-reference
       - Create empty file: touch docs/.gitkeep
       - Create empty file: touch docs/api-reference/.gitkeep

    Validate: Verify python-version matches project requirement (3.12), workflow file is valid YAML, docs/ tree exists.
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-mkdocs.yml'))" && echo "YAML valid" && ls docs/.gitkeep docs/api-reference/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/deploy-mkdocs.yml exists and is valid YAML
    - Workflow triggers: push to main, workflow_dispatch
    - permissions: contents: write (required for gh-deploy)
    - Python version: 3.12 (matches pyproject.toml)
    - Deploy step: "mkdocs gh-deploy --force" (not "mkdocs build")
    - docs/ directory exists with docs/api-reference/ subdirectory
    - docs/.gitkeep and docs/api-reference/.gitkeep exist (empty placeholder files)
  </acceptance_criteria>
  <done>CI workflow file valid; docs/ directories scaffolded with .gitkeep files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → GitHub Pages | gh-pages branch push; uses GITHUB_TOKEN (auto-provisioned, no secrets leaked) |
| Local dev → mkdocs serve | Local-only hot-reload server; no network exposure by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | .github/workflows/deploy-mkdocs.yml | mitigate | Use only GITHUB_TOKEN (auto-provisioned); no API keys or personal access tokens in workflow file |
| T-07-02 | Tampering | mkdocs.yml | mitigate | Config is in version control; any change is reviewable via PR. Site rebuilds on push to main — stale config cannot persist. |
| T-07-SC | Tampering | pip installs in CI | mitigate | All 3 packages pass slopcheck [OK] per RESEARCH.md audit table. No [ASSUMED]/[SUS]/[SLOP] packages. |
</threat_model>

<verification>
Run "uv run mkdocs build --strict" from project root — must exit 0 and produce site/ directory.
Run "python -c \"import yaml; yaml.safe_load(open('.github/workflows/deploy-mkdocs.yml'))\"" — YAML must parse without error.
Verify docs/ and docs/api-reference/ directories exist with .gitkeep files.
</verification>

<success_criteria>
- [ ] uv run mkdocs build --strict exits 0 (docs infrastructure functional)
- [ ] mkdocs.yml at project root with all required configuration sections
- [ ] .github/workflows/deploy-mkdocs.yml valid and complete
- [ ] docs/ directory tree scaffolded (docs/ and docs/api-reference/)
- [ ] pyproject.toml updated with 3 mkdocs dev dependencies
- [ ] uv.lock synchronized
</success_criteria>

<output>
Create `.planning/phases/07-samples-docs-readme-md/07-01-SUMMARY.md` when done
</output>
