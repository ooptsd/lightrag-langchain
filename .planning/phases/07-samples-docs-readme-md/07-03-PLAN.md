---
phase: 07-samples-docs-readme-md
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - README.md
  - docs/index.md
  - docs/quick-start.md
  - docs/examples.md
  - examples/README.md
  - examples/naive_query.py
  - examples/local_query.py
  - examples/global_query.py
  - examples/hybrid_query.py
  - examples/walkthrough.ipynb
autonomous: true
requirements: [D-05]

must_haves:
  truths:
    - "README.md exists at project root and contains Chinese + English technical terms, project description, quick start, feature overview, and links to docs/examples"
    - "All 4 example Python scripts compile without syntax errors (python -m py_compile)"
    - "walkthrough.ipynb is valid JSON with at least 10 cells covering all 6 query modes"
    - "docs/index.md, docs/quick-start.md, docs/examples.md exist with Chinese content"
    - "examples/README.md explains how to set up .env and run examples"
  artifacts:
    - path: "README.md"
      provides: "Bilingual project README with project intro, quick start, feature overview (6 modes), links"
      min_lines: 80
    - path: "docs/index.md"
      provides: "MkDocs landing page with project overview and navigation links"
    - path: "docs/quick-start.md"
      provides: "Step-by-step installation and first query guide"
    - path: "docs/examples.md"
      provides: "Overview of examples/ directory with links to scripts"
    - path: "examples/README.md"
      provides: "Setup instructions for running examples (cp .env.example .env, uv sync, run scripts)"
    - path: "examples/naive_query.py"
      provides: "Self-contained script demonstrating Naive mode (pure vector chunk search)"
    - path: "examples/local_query.py"
      provides: "Self-contained script demonstrating Local mode (entity-centric graph traversal)"
    - path: "examples/global_query.py"
      provides: "Self-contained script demonstrating Global mode (relation-centric graph traversal)"
    - path: "examples/hybrid_query.py"
      provides: "Self-contained script demonstrating Hybrid mode (parallel local + global)"
    - path: "examples/walkthrough.ipynb"
      provides: "Comprehensive Jupyter notebook demonstrating all 6 query modes with output cells"
  key_links:
    - from: "README.md"
      to: "docs/ (MkDocs site)"
      via: "Markdown link to docs/index.md or GitHub Pages URL"
      pattern: "docs/"
    - from: "README.md"
      to: "examples/ directory"
      via: "Markdown link to examples/"
      pattern: "examples/"
    - from: "examples/naive_query.py"
      to: "lightrag_langchain imports"
      via: "from lightrag_langchain import NaiveChain, NaiveRetriever"
      pattern: "from lightrag_langchain import"
    - from: "examples/walkthrough.ipynb"
      to: "6 chain modes"
      via: "Code cells instantiating NaiveChain, LocalChain, etc."
      pattern: "Chain("
---

<objective>
Deliver the complete developer-facing documentation and examples: a bilingual README.md
at project root, 3 user-facing MkDocs Markdown pages (index, quick-start, examples), and
a full examples/ directory with 4 runnable Python scripts, a comprehensive Jupyter notebook,
and setup instructions.

Purpose: Per D-05 (complete examples/ directory), the README language and structure
(Chinese primary + English technical terms, decided at Claude's Discretion), and D-02
(remaining docs structure). The goal: any LangChain developer can read the README,
understand what the library does, and copy-paste examples to start building immediately.

Output: README.md, docs/index.md, docs/quick-start.md, docs/examples.md,
examples/README.md, 4 example scripts, walkthrough.ipynb.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-samples-docs-readme-md/07-CONTEXT.md (README structure and language decisions at Claude's Discretion)
@.planning/phases/07-samples-docs-readme-md/07-RESEARCH.md (Pattern 4: Example Script Structure, lines 357-411; Pattern 5: Jupyter Notebook Structure, lines 413-427)
@src/lightrag_langchain/__init__.py (all public exports — verify which symbols are available)
@src/lightrag_langchain/chain/base.py (LightRAGBaseChain API: invoke, ainvoke, astream params)
@src/lightrag_langchain/chain/chains.py (6 chain subclasses with mode strings)
@src/lightrag_langchain/retriever/retrievers.py (6 retriever subclasses — constructor params)
@src/lightrag_langchain/config.py (PGVectorStore import path is NOT from lightrag_langchain top-level)
@.env.example (existing env template — examples reference this)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md at project root (bilingual: Chinese + English technical terms)</name>
  <files>README.md</files>
  <read_first>
    - .planning/PROJECT.md (project description, core value, constraints)
    - .planning/REQUIREMENTS.md (6 query modes described)
    - .planning/phases/07-samples-docs-readme-md/07-CONTEXT.md (README structure decisions at Claude's Discretion)
    - src/lightrag_langchain/__init__.py (public API surface for feature list)
    - .env.example (for quick start references)
    - pyproject.toml (project name, description, python requirement)
  </read_first>
  <action>
    Create README.md at project root. The language is bilingual: Chinese as primary language with English technical terms kept as-is (e.g. "Chain", "Retriever", "Token Budget").

    Required sections (Claude's Discretion on structure, minimum content specified):

    **1. Title + Badge placeholders**
    - h1: "lightrag-langchain"
    - One-line description: "基于 LangChain 框架的 LightRAG 知识图谱查询层 — 脱离 LightRAG 运行时独立运行，只做查询不做数据写入"
    - Badge placeholders (optional): Python version, license

    **2. 项目简介 (Project Overview)**
    - 3-4 paragraphs in Chinese explaining:
      - What this library is: a LangChain-based read-only query layer for LightRAG's PostgreSQL knowledge graph
      - Core value: users can query LightRAG knowledge graphs through standard LangChain API without running LightRAG service
      - What it provides: 6 query modes (naive, local, global, hybrid, mix, bypass), standard LangChain Retriever + Chain interface
      - Key constraint: read-only (no data writes), PostgreSQL + pgvector + Apache AGE required

    **3. 功能概览 (Feature Overview)**
    - Bulleted list of 6 query modes, each with:
      - **Mode name (English)** — Chinese description: e.g. "Naive — 纯向量相似度搜索 chunks_vdb，不做图遍历"
      - **Naive**: Pure vector similarity search on chunks_vdb, no graph traversal
      - **Local**: Entity-centric — entities_vdb search + AGE graph expansion to neighbors
      - **Global**: Relation-centric — relationships_vdb search + AGE graph entity lookup
      - **Hybrid**: Parallel local + global retrieval with round-robin merge
      - **Mix**: Hybrid + chunk vector search, merging graph knowledge and raw text
      - **Bypass**: Direct LLM call, no retrieval

    **4. 快速开始 (Quick Start)**
    - Step 1: Prerequisites (Python 3.12+, PostgreSQL with pgvector + AGE, uv)
    - Step 2: Install (uv sync or pip install)
    - Step 3: Configure .env (point to .env.example, explain required fields)
      - CRITICAL: Do NOT include real credentials — reference .env.example with placeholder values only.
    - Step 4: 3-line example showing minimal usage:

    ```python
    from lightrag_langchain import NaiveChain, NaiveRetriever, create_llm, create_embedding
    from lightrag_langchain.config import settings

    chain = NaiveChain(retriever=NaiveRetriever(vector_store=..., embedding_config=settings.embedding), llm=create_llm(settings.llm))
    result = await chain.ainvoke("你的问题")
    print(result["answer"])
    ```

    - Use the lazy import form: `from lightrag_langchain import NaiveChain` (NOT `from lightrag_langchain.chain.chains import NaiveChain`)

    **5. 文档和示例 (Docs and Examples)**
    - Link to MkDocs documentation site (placeholder: "https://<user>.github.io/lightrag-langchain/" — update after first deploy per D-06)
    - Link to examples/ directory
    - Note: docs built with MkDocs + Material for MkDocs

    **6. 技术栈 (Tech Stack)**
    - Bulleted list: Python 3.12+, LangChain >= 1.2.3, PostgreSQL + pgvector + AGE, ChatOpenAI-compatible LLM, etc.

    **7. License**
    - Placeholder or actual license

    Writing standards:
    - Chinese text for descriptions, English for code identifiers and technical terms
    - No hardcoded credentials, API keys, or connection strings
    - All code snippets use settings.llm.api_key pattern or .env references, never inline keys
    - Links use relative paths within the repo
  </action>
  <verify>
    <automated>wc -l README.md && grep -c "## " README.md && python -c "import markdown; markdown.markdown(open('README.md').read())" 2>/dev/null && echo "Markdown valid" || echo "Markdown parse check skipped (markdown lib not available)"</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at project root and has at least 80 lines (substantial content)
    - Contains all 7 required sections: Title, Project Overview, Feature Overview, Quick Start, Docs and Examples, Tech Stack, License
    - Quick Start section includes a runnable 3-line code snippet using lazy imports (from lightrag_langchain import ...)
    - Feature Overview lists all 6 query modes with Chinese descriptions
    - No hardcoded credentials, API keys, or real connection strings
    - Chinese is the primary language; English terms are kept for code identifiers and technical concepts
    - File is valid Markdown (parseable)
  </acceptance_criteria>
  <done>README.md created with all required sections, bilingual content, no hardcoded credentials, and runnable quick-start example.</done>
</task>

<task type="auto">
  <name>Task 2: Create MkDocs user-facing pages — index.md, quick-start.md, examples.md</name>
  <files>docs/index.md, docs/quick-start.md, docs/examples.md</files>
  <read_first>
    - README.md (created in Task 1 — reference for tone and structure)
    - src/lightrag_langchain/__init__.py (public API for quick-start example)
    - .env.example (for quick-start configuration reference)
  </read_first>
  <action>
    Create 3 Markdown pages for the MkDocs site. Each page is in Chinese with English technical terms.

    **1. docs/index.md** — Landing page (D-02: "index (README content)")
    Title: "lightrag-langchain"
    Content: A concise landing page (4-5 paragraphs in Chinese):
    - Project one-liner: what it is
    - Core value proposition (3 bullet points)
    - Quick nav links:
      - [快速开始](quick-start.md) — 安装和第一个查询
      - [API 参考](api-reference/index.md) — 完整的 API 文档
      - [示例](examples.md) — 可运行的查询示例
    - Footer note: 文档使用 MkDocs + Material for MkDocs 构建
    - This page should be DIFFERENT from README.md — it is a brief landing page for the docs site, not a full README.
    - Do NOT copy-paste README.md content — keep this page focused on navigation.

    **2. docs/quick-start.md** — Step-by-step guide
    Title: "快速开始 (Quick Start)"
    Sections:
    - 前置条件 (Prerequisites): Python 3.12+, PostgreSQL with pgvector + AGE, uv
    - 安装 (Installation): uv sync or pip install
    - 配置 .env (Configuration): step-by-step with minimal .env example (reference .env.example)
    - 第一个查询 (First Query): 5-10 line code snippet showing NaiveChain usage
    - 下一步 (Next Steps): links to API reference and examples

    The code snippet in "第一个查询" must use lazy imports:
    ```python
    from lightrag_langchain import NaiveChain, NaiveRetriever, create_llm, create_embedding
    from lightrag_langchain.config import settings
    from lightrag_langchain.data.store import PGVectorStore

    # ... setup PGVectorStore ...
    chain = NaiveChain(retriever=NaiveRetriever(...), llm=create_llm(settings.llm))
    result = await chain.ainvoke("你的问题")
    ```

    **3. docs/examples.md** — Examples directory overview
    Title: "示例 (Examples)"
    Content:
    - Brief Chinese paragraph explaining the examples/ directory
    - Bulleted list of available examples:
      - naive_query.py — Naive 模式（纯向量搜索）
      - local_query.py — Local 模式（实体中心图扩展）
      - global_query.py — Global 模式（关系中心图扩展）
      - hybrid_query.py — Hybrid 模式（并行 local + global）
      - walkthrough.ipynb — 完整演示（所有 6 种查询模式）
    - Link to examples/README.md for setup instructions
    - Note: Bypass 模式直接调用 LLM，无需检索，演示见 walkthrough.ipynb

    After writing all 3 files, run "uv run mkdocs build --strict" to verify.
  </action>
  <verify>
    <automated>ls docs/index.md docs/quick-start.md docs/examples.md && uv run mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - docs/index.md exists as a brief landing page (different from README.md, navigation-focused)
    - docs/quick-start.md exists with Prerequisites, Installation, Configuration, First Query, Next Steps sections
    - docs/examples.md exists with links to all 4 scripts + walkthrough.ipynb
    - All pages are in Chinese with English technical terms
    - "uv run mkdocs build --strict" exits 0
  </acceptance_criteria>
  <done>3 MkDocs user-facing pages created; mkdocs build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Create complete examples/ directory — 4 Python scripts + walkthrough.ipynb + README</name>
  <files>
    examples/README.md,
    examples/naive_query.py,
    examples/local_query.py,
    examples/global_query.py,
    examples/hybrid_query.py,
    examples/walkthrough.ipynb
  </files>
  <read_first>
    - .planning/phases/07-samples-docs-readme-md/07-RESEARCH.md (Pattern 4: Example Script Structure, lines 357-411; Pattern 5: Jupyter Notebook Structure, lines 413-427)
    - .planning/phases/07-samples-docs-readme-md/07-CONTEXT.md (D-05: examples scope; Claude's Discretion on bypass)
    - src/lightrag_langchain/__init__.py (all public imports for example scripts)
    - src/lightrag_langchain/retriever/retrievers.py (constructor params for each retriever type)
    - .env.example (reference for examples/.env.example placeholder)
  </read_first>
  <action>
    Create the examples/ directory with 6 files. First run: mkdir -p examples

    **1. examples/README.md** — Setup instructions (Chinese, ~20-30 lines)
    Sections:
    - 前置条件: Python 3.12+, PostgreSQL with data, .env configured
    - 配置: cp .env.example .env (edit with your credentials). If root .env.example exists, instruct user to copy from there. Otherwise create a symlink or minimal template.
    - 运行示例: python naive_query.py or uv run python naive_query.py
    - Notebook: pip install jupyter (optional), then jupyter notebook walkthrough.ipynb
    - 查询模式说明: table listing 5 example files and which mode each demonstrates
    - Note: Bypass 模式不需要检索，直接调用 LLM — 见 walkthrough.ipynb

    **2-5. Four Python scripts** — each follows RESEARCH.md Pattern 4 template exactly:

    All scripts share this structure (customize mode-specific parts):
    - Shebang/docstring header with Chinese description and usage instructions
    - sys.path.insert for running from examples/ directory
    - Imports: lazy form for chains/retrievers/factories (from lightrag_langchain import ...)
    - Imports: direct form for data layer (from lightrag_langchain.data.store import PGVectorStore)
    - async main() function with 5 labeled sections: (1) Create data connections, (2) Create LLM/embedding, (3) Build retriever, (4) Build chain, (5) Query
    - if __name__ == "__main__": asyncio.run(main())

    **examples/naive_query.py** — Naive mode
    - Retriever: NaiveRetriever(vector_store=vector_store, embedding_config=settings.embedding)
      No graph_store needed (naive mode is pure vector search)
    - Chain: NaiveChain(retriever=retriever, llm=llm)
    - Query: await chain.ainvoke("启动东莞市防风Ⅰ级应急响应后需要落实哪些措施？")
    - Print: mode, keywords (high_level + low_level), source count, answer

    **examples/local_query.py** — Local mode
    - Imports: from lightrag_langchain.data.graph import PGGraphStore
    - Data: both vector_store and graph_store
    - Retriever: LocalRetriever(vector_store=vector_store, graph_store=graph_store, embedding_config=settings.embedding)
    - Chain: LocalChain(retriever=retriever, llm=llm)
    - Query: await chain.ainvoke("珠江流域超标准洪水时水库抢险标准是什么？")
    - Print: mode, keywords, source count, answer

    **examples/global_query.py** — Global mode
    - Same structure as local_query.py
    - Retriever: GlobalRetriever(vector_store=vector_store, graph_store=graph_store, embedding_config=settings.embedding)
    - Chain: GlobalChain(retriever=retriever, llm=llm)
    - Query: await chain.ainvoke("哪些机构参与了防汛应急响应？")
    - Print: same format

    **examples/hybrid_query.py** — Hybrid mode
    - Same structure as local_query.py
    - Retriever: HybridRetriever(vector_store=vector_store, graph_store=graph_store, embedding_config=settings.embedding)
    - Chain: HybridChain(retriever=retriever, llm=llm)
    - Query: await chain.ainvoke("防风应急响应和防汛应急响应有何异同？")
    - Print: same format

    Important for all scripts:
    - Use lazy imports: from lightrag_langchain import NaiveChain (NOT from lightrag_langchain.chain.chains import NaiveChain)
    - Use direct data imports: from lightrag_langchain.data.store import PGVectorStore (data classes are NOT re-exported)
    - Do NOT use settings embedding model directly — pass settings.embedding (the EmbeddingConfig) to the retriever, which lazily creates the embedding model
    - Include a brief docstring at the top of each script explaining what the mode does
    - Add a comment explaining the mixed import pattern (chains/retrievers use lazy imports; data connections require direct data-layer access)

    **6. examples/walkthrough.ipynb** — Comprehensive Jupyter notebook
    Create as valid nbformat v4 JSON. Structure (per RESEARCH Pattern 5):
    - Cell 1 (Markdown): Title "lightrag-langchain 完整演示" + project intro paragraph
    - Cell 2 (Markdown): "前置条件" — PostgreSQL with LightRAG data, .env configured
    - Cell 3 (Code): Imports + settings check
    - Cell 4 (Code): Create shared connections (vector_store, graph_store, llm, embedding)
    - Cell 5 (Markdown): "Naive 模式 — 纯向量搜索"
    - Cell 6 (Code): NaiveChain setup + query + output display
    - Cell 7 (Markdown): "Local 模式 — 实体中心图扩展"
    - Cell 8 (Code): LocalChain setup + query + output display
    - Cell 9 (Markdown): "Global 模式 — 关系中心图扩展"
    - Cell 10 (Code): GlobalChain setup + query + output display
    - Cell 11 (Markdown): "Hybrid 模式 — 并行 local + global"
    - Cell 12 (Code): HybridChain setup + query + output display
    - Cell 13 (Markdown): "Mix 模式 — hybrid + chunk 搜索"
    - Cell 14 (Code): MixChain setup + query + output display
    - Cell 15 (Markdown): "Bypass 模式 — 直接 LLM 调用"
    - Cell 16 (Code): BypassChain setup + query + output display
    - Cell 17 (Markdown): "总结" — 6 modes comparison table

    Notebook format: valid nbformat v4 JSON. Each code cell has "outputs": [], "execution_count": null.
    Use nbformat v4 metadata: "nbformat": 4, "nbformat_minor": 5.

    Do NOT add jupyter as a project dependency. The notebook is a standalone .ipynb file.
    Note in examples/README.md: "要运行 walkthrough.ipynb，请安装 jupyter: pip install jupyter"

    After creating all files, verify:
    - python -m py_compile examples/*.py (all scripts compile)
    - python -c "import json; json.load(open('examples/walkthrough.ipynb'))" (valid JSON)
  </action>
  <verify>
    <automated>python -m py_compile examples/*.py && echo "All scripts compile OK" && python -c "import json; nb=json.load(open('examples/walkthrough.ipynb')); print(f'Notebook: {nb[\"nbformat\"]}.{nb[\"nbformat_minor\"]}, {len(nb[\"cells\"])} cells')" && ls examples/README.md</automated>
  </verify>
  <acceptance_criteria>
    - examples/README.md exists with setup instructions (Chinese, .env configuration, run commands)
    - examples/naive_query.py exists and compiles (python -m py_compile passes)
    - examples/local_query.py exists and compiles
    - examples/global_query.py exists and compiles
    - examples/hybrid_query.py exists and compiles
    - All 4 scripts use lazy imports: from lightrag_langchain import {Chain}Chain, {Chain}Retriever
    - All 4 scripts import data classes from direct path: from lightrag_langchain.data.store import PGVectorStore
    - examples/walkthrough.ipynb is valid nbformat v4 JSON with at least 16 cells
    - Notebook covers all 6 query modes (Naive, Local, Global, Hybrid, Mix, Bypass)
    - No hardcoded credentials in any example file
    - Bypass mode is in walkthrough.ipynb only (no standalone bypass_query.py — per Claude's Discretion)
  </acceptance_criteria>
  <done>Complete examples/ directory: 4 compile-clean scripts, comprehensive notebook covering all 6 modes, setup README.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Example scripts → .env | Scripts reference .env for credentials; never inline API keys |
| README.md → Public | Visible on GitHub repo landing page; must not contain secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | examples/*.py example scripts | mitigate | All scripts use settings.llm.api_key pattern or .env references. Verify with grep that no script contains "sk-" or actual-looking API keys. |
| T-07-06 | Information Disclosure | README.md | mitigate | Verify no "sk-" pattern or real credentials in README. Quick start references .env.example with placeholder values. |
| T-07-07 | Information Disclosure | walkthrough.ipynb | mitigate | Same as scripts — notebook code cells use settings pattern, not inline keys. |
</threat_model>

<verification>
Run "python -m py_compile examples/*.py" — all 4 scripts must compile without errors.
Run "python -c \"import json; json.load(open('examples/walkthrough.ipynb'))\"" — notebook must be valid JSON.
Run "uv run mkdocs build --strict" — must exit 0 with all new docs pages.
Run "grep -r 'sk-' README.md examples/ docs/" — must return empty (no hardcoded API keys anywhere).
</verification>

<success_criteria>
- [ ] README.md exists with all required sections, bilingual, no credentials
- [ ] docs/index.md, docs/quick-start.md, docs/examples.md exist and render via MkDocs
- [ ] 4 example Python scripts compile and follow the lazy import pattern
- [ ] walkthrough.ipynb is valid nbformat v4 JSON with >= 16 cells covering all 6 modes
- [ ] examples/README.md explains setup and running instructions
- [ ] uv run mkdocs build --strict exits 0
- [ ] No hardcoded credentials across all documentation and example files
</success_criteria>

<output>
Create `.planning/phases/07-samples-docs-readme-md/07-03-SUMMARY.md` when done
</output>
